- [2026-09-03] | Trocas automáticas ganharam indicação numérica e confirmação operacional | O plano matemático existente continua distribuindo igualmente o tempo dos jogadores de linha conforme tempo útil, quantidade de atletas e ordem numérica, mas deixou de aplicar trocas silenciosamente pelo relógio. A súmula agora mostra abaixo do campo a próxima troca de cada time, horário e pares `Sai #N nome → Entra #N nome`; ao atingir o horário, libera `FAZER TROCA`, que atualiza titulares/banco, registra nomes e números no log e persiste `startsOnBench` pelo autosave. A composição salva permite inferir passos já concluídos ao reabrir a súmula. Troca manual continua pausando o automático do time. No mobile, Time A e Time B são empilhados. RCA 056 criado; typecheck, build, diagnósticos e diff check concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000056_trocas_automaticas_sem_controle_operacional.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar rodízios com diferentes quantidades de linhas, troca no horário, passos atrasados, reabertura da súmula e pausa por troca manual.
- [2026-09-03] | Colunas da Agenda redistribuídas e refinadas | A largura excessiva de Jogo aproximava seu início de Data e empurrava Confirmação para longe. Após refinamento visual, a grade fixa vigente passou de 22%/38%/23%/17% para 30%/26%/27%/17% nas telas maiores e de 24%/35%/24%/17% para 32%/22%/29%/17% no mobile. Jogo agora começa de 4 a 5 pontos percentuais mais à direita e fica proporcionalmente mais próximo de Confirmação, mantendo Ações e 100% da tabela. RCA 055 atualizado; typecheck, build e diagnósticos do frontend concluídos sem erros. | frontend/src/styles.css, RCA000000000055_distribuicao_colunas_agenda.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar títulos curtos e longos em mobile e desktop.
- [2026-09-03] | Header global ocultado durante qualquer modal | O header sticky tinha `z-index: 90`, acima do overlay `.modal` com `z-index: 10`, permanecendo visível e interceptando o clique no X. A shell agora oculta o header e desabilita seus eventos sempre que contém um modal; ao fechar, o header retorna automaticamente. A solução é global para modais existentes e futuros que reutilizem `.modal`. RCA 054 criado; typecheck, build e diagnósticos do frontend concluídos sem erros. | frontend/src/styles.css, RCA000000000054_header_sobrepoe_fechamento_dos_modais.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar abertura/fechamento de modais no mobile e desktop.
- [2026-09-03] | Financeiro histórico, mensalidades prioritárias e login inadimplente | A Gestão financeira administrativa passou a ser exclusiva de ADMIN; ATLETA e COORDENADOR preservam apenas `Minha mensalidade`. Listagem e resumo administrativos deixaram o filtro de temporada, mantendo dívidas, mensalidades, receitas, despesas e saldo do caixa através do encerramento e troca de temporadas, enquanto `season_id` continua preservado para vínculo esportivo e pontuação. A expansão por atleta mostra no máximo a cobrança vencida mais antiga e a próxima aberta não vencida, avançando sobre competências já quitadas; totais consideram todo o histórico. Novos logins de ATLETA/COORDENADOR com saldo vencido são bloqueados, com exceção de ADMIN para regularização. Status mobile foram mantidos em uma linha sem recorte. RCA 053 criado; typechecks e builds de frontend/backend concluídos sem erros. | backend/src/routes/auth.routes.ts, backend/src/routes/payments.routes.ts, backend/src/routes/cash.routes.ts, frontend/src/App.tsx, frontend/src/styles.css, RCA000000000053_mensalidades_historicas_login_e_status_financeiro.md, PROJECT_CONTEXT.md | Publicar backend e frontend; homologar troca de temporada, pagamentos adiantados, múltiplos atrasos, login bloqueado e exceção ADMIN.
- [2026-09-03] | Gerenciar pré-jogo temporariamente ocultado | O botão `Gerenciar pré-jogo`, seu estado de abertura e o modal administrativo foram retirados da interface enquanto o processo é alinhado internamente. Backend, migration 19, componente já desenvolvido e visualização somente leitura da escalação em T-1h foram preservados para possível retomada sem perda do trabalho. Typecheck do frontend concluído sem erros. | frontend/src/App.tsx, PROJECT_CONTEXT.md | Alinhar o processo com a gestão antes de reativar o acesso administrativo ao pré-jogo.
- [2026-09-03] | Gerenciar pré-jogo concentra confirmados, suplentes e sorteio | O modal passou a exibir permanentemente a lista nominal de quem confirmou para jogar, numerada e acompanhada da posição; convidados adicionados aparecem na mesma lista identificados como suplentes. Quando há déficit, o bloco `Adicionar suplentes` recebe nome e posição; ao atingir o quórum, o bloco `Sortear os times` oferece a ação no mesmo modal. Após o sorteio, times, fila e substituições permanecem disponíveis. Typecheck, build e diagnósticos do frontend concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Publicar o frontend e homologar o modal com lista vazia, confirmados do clube, suplentes e 20 vagas completas.
- [2026-09-03] | Fluxo pré-jogo de 20 vagas, reservas e sorteio implementado | Jogos novos deixam de ter convocação ou inclusão antecipada de suplente: todos os usuários ativos podem confirmar até T-3h. Depois do corte, a coordenação completa somente vagas faltantes com convidados confirmados; com pelo menos 20 elegíveis, o backend seleciona exatamente 20 por ordem pseudoaleatória auditável, ordena excedentes como reservas, balanceia dois times de 10 por posição e congela o resultado. Ausências posteriores são substituídas no mesmo time e posição por reserva ou convidado, sem ressorteio. A escalação fica visível em T-1h somente para selecionados/reservas e em modo leitura; coordenação mantém o board operacional. Endpoints usam transação e bloqueio de partida, e o início oficial foi limitado ao horário agendado. A migration 19 é manual, idempotente e mantém partidas antigas no fluxo legado. Typechecks e builds de frontend/backend concluídos sem erros. | .gitignore, migrations/19_fluxo_pre_jogo_20_vagas.sql, backend/src/routes/matches.routes.ts, frontend/src/App.tsx, frontend/src/types.ts, frontend/src/styles.css, PROJECT_CONTEXT.md | Aplicar manualmente a migration 19 no PostgreSQL da Railway; publicar backend e frontend; homologar T-3h, 19/20/21 confirmados, convidados, reservas, substituição e visibilidade em T-1h.
- [2026-09-03] | Loading inicial com spinner e sessão expirada retorna ao login | Durante chamadas à Railway, todas as views autenticadas ficam desmontadas e somente um spinner verde central é exibido, sem a frase antiga ou cards provisórios como `Sem próximo jogo operacional`. O loading inicia junto da autenticação salva e do login. O `ApiClient` agora trata 401 autenticado globalmente, invalida o token, remove a sessão local e abre o login imediatamente, inclusive para polling. RCA 052 criado; typecheck concluído sem erros. | frontend/src/App.tsx, frontend/src/api.ts, frontend/src/styles.css, RCA000000000052_loading_inicial_e_sessao_expirada.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar carga inicial lenta e JWT expirado em navegação e polling.
- [2026-09-03] | Perfil reduzido à composição de pontos | A grade de Resumo da carreira com pontos, presenças, gols, assistências, vitórias e cartões foi removida, assim como seu título. Depois da identidade e do radar, o perfil exibe somente a linha compacta `Ver composição dos pontos`, expansível sob demanda. Estilos exclusivos da grade foram eliminados. RCA 051 refinado. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000051_perfil_com_graficos_excedentes_e_pontos_sem_origem.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar o perfil fechado e a composição expandida em mobile e desktop.
- [2026-09-03] | Origem dos pontos movida para dropdown compacto | A memória de cálculo deixou de ocupar uma box permanente no perfil. Dentro do Resumo da carreira, um `details/summary` mostra apenas `Ver composição dos pontos` e o total; ao tocar ou clicar, expande presença, pagamentos, vitórias, empates, derrotas e ajustes em linhas sem outro card externo. O controle funciona com mouse, toque e teclado nativamente. RCA 051 refinado. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000051_perfil_com_graficos_excedentes_e_pontos_sem_origem.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar o dropdown fechado/aberto em mobile e desktop.
- [2026-09-03] | Header global ocultado em todos os perfis | A condição foi ampliada: enquanto existir qualquer `profileUserId`, o header global com logo e menu não é renderizado. Isso vale para o perfil próprio e para perfis de outros atletas; ao fechar o modal, o header retorna normalmente. RCA 051 refinado. | frontend/src/App.tsx, RCA000000000051_perfil_com_graficos_excedentes_e_pontos_sem_origem.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar abertura/fechamento pelo Meu perfil, destaques e tabelas.
- [2026-09-03] | Header global ocultado no perfil próprio | Enquanto `profileUserId` corresponde ao usuário autenticado, o header global com logo e menu deixa de ser renderizado, mantendo apenas o perfil e seu X de fechamento. Perfis de terceiros preservam a navegação global. RCA 051 refinado. | frontend/src/App.tsx, RCA000000000051_perfil_com_graficos_excedentes_e_pontos_sem_origem.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar abertura/fechamento de Meu perfil e perfil de terceiros.
- [2026-09-03] | Perfil simplificado e origem dos pontos detalhada | O cabeçalho `Perfil do atleta`, histórico de temporadas, gráfico de evolução, títulos e badges saíram do modal compartilhado. O próprio perfil e perfis de terceiros agora mantêm identidade, radar e resumo da carreira. O endpoint de carreira passou a fornecer jogos disputados, pagamentos pontuáveis e as regras atuais de `point_settings`; o resumo mostra presença, pagamento, vitória, empate e derrota como quantidade × valor configurado, reconciliando importações/ajustes com o total oficial. Fallbacks preservam compatibilidade durante deploy separado. RCA 051 criado; typechecks de frontend/backend e build do frontend concluídos sem erros. | backend/src/routes/users.routes.ts, frontend/src/App.tsx, frontend/src/styles.css, RCA000000000051_perfil_com_graficos_excedentes_e_pontos_sem_origem.md, PROJECT_CONTEXT.md | Publicar backend e frontend; homologar perfil próprio e de terceiro com regras positivas, negativas, zeradas e saldo importado.
- [2026-09-03] | Colunas de Temporadas compactadas no mobile | Um override responsivo posterior ainda restaurava `min-width: 860px`, largura pelo conteúdo e layout automático na tabela administrativa. A regra final de Temporadas agora neutraliza esse legado: wrapper sem overflow horizontal, tabela em 100% com mínimo zero e layout fixo, colunas Temporada/Ano/Status em 50%/17%/33%, padding reduzido e Ano/Status alinhados sob os cabeçalhos. RCA 050 refinado. | frontend/src/styles.css, RCA000000000050_tabelas_configuracoes_com_rolagem_lateral.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar temporadas com nomes e status longos em 375x823 e 390x844.
- [2026-09-03] | Colunas da Pontuação configurável aproximadas e alinhadas | O modal deixou a geometria compartilhada de até 980px e passou a ter no máximo 460px. A coluna Regra ocupa o espaço restante, enquanto Pontos usa 92px e campos de 72px centralizados exatamente sob o cabeçalho. Wrapper, tabela e modal neutralizam overflow horizontal nesse fluxo. RCA 050 refinado. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000050_tabelas_configuracoes_com_rolagem_lateral.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar nomes curtos e longos em mobile e desktop.
- [2026-09-03] | Configurações sem barras laterais e temporadas com detalhes | A ação Importar Excel foi removida da área de Configurações. Pontuação agora exibe somente nome e pontos em duas colunas responsivas. Temporadas foi reduzida a nome, ano e status; clicar ou usar Enter/Espaço em uma linha abre um modal com período, votação, status e as ações de iniciar ou encerrar aplicáveis. Ambas as tabelas usam 100%, layout fixo e nenhum mínimo horizontal. RCA 050 criado; typecheck e build concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000050_tabelas_configuracoes_com_rolagem_lateral.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar Pontuação e Temporadas em mobile e desktop.
- [2026-09-03] | Botões de Configurações compactados | As quatro ações administrativas deixaram de reutilizar cards de largura total e 92px de altura. Criar temporada, Pontuação, Novo usuário e Importar Excel agora são botões verdes pequenos, alinhados em linha com quebra responsiva e largura pelo conteúdo; modais, handlers, permissões e bloqueio sem temporada ativa foram preservados. RCA 049 criado. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000049_botoes_configuracoes_superdimensionados.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar as ações de Configurações em mobile e desktop.
- [2026-09-02] | Rolagem vertical completa da Agenda restaurada | O wrapper final da tabela combinava `overflow-x: auto` e `overflow-y: hidden`, mantendo um contêiner de recorte que impedia a página de alcançar todas as linhas. A Agenda agora mantém ambos os eixos visíveis: a tabela cresce por todos os agendamentos, a rolagem vertical pertence ao documento, o header global continua sticky e a contenção horizontal permanece garantida pela largura de 100%, layout fixo e células responsivas. RCA 048 criado; build do frontend concluído sem erros. | frontend/src/styles.css, RCA000000000048_rolagem_vertical_agenda_bloqueada.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar a Agenda com quantidade de linhas superior à altura da viewport em mobile e desktop.
- [2026-09-02] | Agenda compactada e duplicidade de jogos bloqueada | A tabela da Agenda passou de sete para quatro colunas: Data/Horário, Jogo, Confirmação e Ações, com largura fixa de 100% e sem rolagem lateral. Janela, origem, equipes, regra, presenças, janta e ações completas foram movidas para um modal aberto ao clicar no agendamento. Registros `DRAFT` com mesma temporada, data e horário são agrupados em uma linha e sinalizados; o modal lista cada registro para revisão e remoção consciente, sem apagar dados automaticamente. No backend, criação manual, recorrente, edição e o fluxo geral Criar jogo usam advisory lock transacional e consulta de conflito por temporada/data/horário, impedindo concorrência e novos horários duplicados. Nenhuma migration foi necessária. RCA 047 criado; typechecks de frontend/backend e build do frontend concluídos sem erros. | frontend/src/App.tsx, frontend/src/types.ts, frontend/src/styles.css, backend/src/routes/matches.routes.ts, RCA000000000047_agenda_compacta_e_jogos_duplicados.md, PROJECT_CONTEXT.md | Publicar frontend e backend; homologar tabela em 375x823, detalhes, remoção individual de repetidos e conflito 409 em criação manual, recorrente, edição e Criar jogo.
- [2026-09-02] | Tabela da Agenda restaurada e editor compactado | A mudança para páginas de altura natural revelou dependência antiga da Agenda em `flex: 1 1 0`: sem pai de altura fixa, o wrapper da tabela podia colapsar. A Agenda agora usa linhas automáticas e a tabela cresce conforme os agendamentos, mantendo somente rolagem horizontal. Os botões `Recorrente`, `Data específica` e `Editar` abrem um editor de até 520px no desktop e 440px no mobile, limitado à viewport, com campos compactos e cabeçalho sticky. O formulário fecha pelo X, clique externo e `Esc`. Endpoints e regras de agenda não foram alterados. RCA 046 criado; typecheck e build concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000046_agenda_tabela_oculta_e_modal_gigante.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar tabela vazia/preenchida, recorrência, data específica, edição e os três mecanismos de fechamento no mobile e desktop.
- [2026-09-02] | Rolagem interna por view supersedida | A primeira interpretação do RCA 044 limitava a shell a `100dvh` e criava uma barra vertical dentro do container de cada view. Esse comportamento foi rejeitado porque tabelas e listas devem crescer por todas as linhas e a barra vertical deve pertencer à página. A decisão vigente está no RCA 045 e no registro posterior. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000044_rolagem_vertical_padronizada_paginas.md, PROJECT_CONTEXT.md | Considerar como vigente somente a política de altura natural e scroll do documento registrada abaixo.
- [2026-09-02] | Página com altura natural e header global sticky | Todas as views autenticadas agora crescem conforme seu conteúdo e usam uma única barra vertical do documento. O header com menu e logo permanece sticky no topo durante a rolagem. Limites de altura e scroll vertical interno foram removidos de tabelas e listas de Temporada, Partidas, Tabela da temporada, Agenda, Gestão financeira, Prêmios, Usuários e Configurações; assim, por exemplo, a tabela de Usuários compõe a página com todas as pessoas. A rolagem horizontal necessária para tabelas largas foi preservada. RCA 045 criado; build de produção concluído sem erros. | frontend/src/styles.css, RCA000000000045_rolagem_interna_em_vez_de_pagina.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar a barra vertical única, o header sticky e todas as linhas das tabelas em mobile e desktop.
- [2026-09-02] | Falha externa de autenticação do Docker Hub diagnosticada | O deployment `7aab6f50-a8a4-4ceb-a9aa-584bdad840fe` do frontend falhou antes das etapas do projeto ao buscar a imagem pública `nginx:1.27-alpine`: `auth.docker.io/token` respondeu HTTP 500 ao builder da Railway. O Dockerfile foi conferido e a mesma URL consultada posteriormente respondeu HTTP 200 com token válido, confirmando indisponibilidade transitória externa. Nenhum código ou Dockerfile foi alterado; a recuperação correta é executar Redeploy no serviço frontend. RCA 043 criado. | frontend/Dockerfile, RCA000000000043_railway_falha_autenticacao_docker_hub.md, PROJECT_CONTEXT.md | Executar Redeploy do frontend na Railway e confirmar que a obtenção das imagens base avança para `npm ci` e `npm run build`.
- [2026-09-02] | Usuários compactados sem rolagem lateral | A tabela compartilhada de Usuários passou de sete para quatro colunas: Nome, Posição, Status e Ações. E-mail, Perfil e Retorno foram removidos da visualização; edição e reenvio de convite permanecem operacionais. O nome usa primeiro nome e inicial do primeiro sobrenome, preservando o nome completo no tooltip. A tabela agora ocupa 100%, sem largura mínima ou barra lateral, com ações contidas na própria célula. O resumo foi organizado em duas linhas: cadastrados, ativos e atletas na primeira; coordenadores e administradores na segunda. Após homologação visual, a coluna Posição mobile foi ampliada para 24%, Status reduzida para 13% e os badges `ativo`/`inativo` passaram a ter largura natural, fonte e padding menores, eliminando a aproximação entre posição e status. RCA 042 atualizado; typecheck e builds concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000042_usuarios_mobile_colunas_e_resumo.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar posições longas, status ativo/inativo, filtros, edição e reenvio de convite em 375x823 e 390x844.
- [2026-09-02] | Fechamento dos modais padronizado com X único | Todos os controles de fechamento no cabeçalho dos modais agora exibem um `X` de 32px, sem texto `Fechar` visível. O conteúdo legado (`Fechar`, `X` explícito ou vazio) é ocultado e um único pseudo-elemento desenha o ícone, evitando duplicação mesmo após as regras mobile de tipografia. Nomes acessíveis e `aria-label` existentes foram preservados; novos modais devem usar `.modal-close-button`. RCA 041 criado; build concluído sem erros. | frontend/src/styles.css, RCA000000000041_fechamento_modais_nao_padronizado.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar visualmente um único X nos modais mobile e desktop.
- [2026-09-02] | Modais comuns compactados no mobile | A regra global que esticava overlays e cards para 100% da largura e quase 100dvh foi neutralizada para modais comuns. Cards agora ficam centralizados, com largura máxima de 520px e margem de 12px, altura pelo conteúdo, máximo de `100dvh - 24px`, padding de 10px, gap de 7px e raio de 10px. Inputs/selects usam 32px, botões 30px, textareas 58px e formulários em duas colunas acima de 420px, retornando a uma coluna nas telas menores. Detalhes financeiros também foram compactados. Súmula, criação de jogo e editor de agenda mantêm geometria ampla por necessidade operacional. RCA 040 criado; build concluído sem erros. A inspeção visual automatizada não foi executada porque o início do servidor local foi dispensado. | frontend/src/styles.css, RCA000000000040_modais_mobile_superdimensionados.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar modais de senha, presença, financeiro, prêmios, usuários e configurações em 375x823 e 390x844.
- [2026-09-02] | Central de Prêmios convertida em lista com salvamento direto | O botão `Salvar central` foi removido. `Adicionar regra` recebeu verde explícito e abre um modal compacto somente com nome, métrica e ícone; novas regras são criadas como ranking automático e persistidas imediatamente. A tabela deixou a interface e foi substituída por uma lista compacta com ícone, nome, métrica e estado. Ao clicar em uma regra, abre um modal com suas configurações aplicáveis e `Salvar alterações` persiste diretamente. `Tipo` e `Dica` não aparecem mais na interface principal; tipos históricos são preservados internamente. Campos usam 34px de altura, criação tem 440px e edição 560px, ambas responsivas. RCA 039 criado; typecheck e build concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000039_central_premios_tabela_e_salvamento_global.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar criação, edição de ranking, edição de votação, ativação e falha de API em mobile e desktop.
- [2026-09-02] | Tabela de Prêmios compactada e criação em modal | A Central de regras deixou de exibir permanentemente a linha de criação. O botão `Adicionar regra` no cabeçalho abre um modal com nome, tipo, métrica condicional e ícone; o modal fecha pelo botão, clique externo, Esc ou após inclusão válida, preservando validação de nome duplicado. `Salvar central` continua persistindo regras novas e alterações existentes. No mobile, a tabela passa a mostrar somente `Regra`, `Tipo`, `Métrica/Votação` e `Status`, nas larguras 31%, 23%, 28% e 18%; parâmetros secundários permanecem editáveis no desktop. A tabela usa 100%, layout fixo, controles contidos e sem rolagem horizontal. RCA 038 criado; typecheck e build concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000038_tabela_premios_mobile_e_criacao_inline.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar criação, duplicidade, salvamento e tabela com regras longas em 375x823, 390x844 e desktop.
- [2026-09-02] | Mensalidades abertas compactadas no mobile | A subtabela expandida por atleta removeu as colunas `Pendente` e `Observação`, preservando `Mês`, `Vencimento`, `Valor`, `Pago`, `Status` e `Ação`. O mínimo padrão caiu de 860px para 680px e, até 820px, a subtabela usa `width: 100%`, `min-width: 0`, `table-layout: fixed` e larguras de 19%, 20%, 16%, 14%, 14% e 17%. O botão de baixa ocupa somente sua coluna e quebra texto quando necessário, mantendo as informações operacionais visíveis sem corte lateral. RCA 037 criado; typecheck, build e diagnósticos concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000037_mensalidades_abertas_cortadas_mobile.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar atletas com uma e várias mensalidades abertas em 375x823 e 390x844.
- [2026-09-02] | Descrição do Caixa centralizada no mobile | Após validação por captura, foi removido o recuo mobile que mantinha `teste` e `Avulso` à esquerda do cabeçalho `Descrição`. A terceira coluna agora centraliza rótulo e valores no mesmo eixo, com padding lateral de 1px, preservando largura de 25%, quebra de texto e ausência de rolagem horizontal. O desktop mantém descrição à esquerda. RCA 036 corrigido; build de produção concluído sem erros. | frontend/src/styles.css, RCA000000000036_descricao_caixa_desalinhada.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar o centro da terceira coluna em 375x823 e 390x844.
- [2026-09-02] | Blocos da Gestão financeira abrem detalhamento | Os indicadores visíveis `recebido`, `pendentes`, `atrasos`, `receitas`, `despesas` e `saldo caixa` passaram a ser botões. Cada bloco abre um modal responsivo com total, quantidade e registros que compõem o indicador; mensalidades exibem atleta, status, mês, vencimento, pago e saldo, enquanto caixa exibe descrição, tipo, data, origem, responsável e valor. O saldo apresenta todas as movimentações. O modal fecha pelo botão, clique externo ou Esc e reutiliza os dados deduplicados já carregados, sem nova API. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Publicar o frontend e homologar os seis blocos com listas vazias, listas extensas e valores longos em mobile e desktop.
- [2026-09-02] | Colunas do Caixa do grupo alinhadas no mobile | As seis colunas foram redistribuídas para Data 14%, Tipo 12%, Descrição 25%, Origem 13%, Responsável 17% e Valor 19%. Células e cabeçalhos agora contêm overflow, dados ficam centralizados salvo descrição à esquerda, badges respeitam `max-width: 100%` e os filtros usam grade interna de 14px com rótulo quebrável. A tabela continua sem rolagem lateral. RCA 035 criado; build e diagnósticos concluídos sem erros, com regras confirmadas no CSS gerado. | frontend/src/styles.css, RCA000000000035_colunas_caixa_grupo_sobrepostas_mobile.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar lançamentos com descrições, responsáveis e valores longos em 375x823 e 390x844.
- [2026-09-02] | Indicador aberto removido da Gestão financeira | O card monetário `aberto` foi retirado exclusivamente do resumo superior, preservando a coluna `Próximo mês em aberto` da tabela por atleta para uso operacional. O indicador de pontos antecipados continua oculto. RCA 034 criado; build e diagnósticos concluídos sem erros. | frontend/src/styles.css, RCA000000000034_indicador_aberto_gestao_financeira.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar o resumo financeiro em mobile e desktop.
- [2026-09-02] | Legibilidade da tabela da temporada ampliada | A compactação horizontal foi preservada sem barra lateral, mas a tipografia mobile foi ampliada: valores para `.76rem`, nomes para `.74rem`, cabeçalhos para `.7rem` e padding vertical para 8px. A coluna Atleta continua com 96px, reticências e tooltip, impedindo sobreposição sobre `PTS`. RCA 033 criado; build e diagnósticos concluídos sem erros. | frontend/src/styles.css, RCA000000000033_tabela_temporada_mobile_pequena.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar leitura e ausência de overflow em 375x823 e 390x844.
- [2026-09-02] | Nomes longos contidos na tabela da temporada | A coluna Atleta mobile preserva 96px e agora contém explicitamente seu botão em `100%`, substitui a fonte global de 1rem pela fonte compacta de `.66rem` e aplica reticências para impedir que nomes longos cubram `PTS`. O nome completo permanece no tooltip e o clique para perfil foi preservado, sem reintroduzir rolagem lateral. RCA 032 criado; build e diagnósticos concluídos sem erros. | frontend/src/styles.css, RCA000000000032_nome_sobrepoe_pontos_tabela_temporada.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar nomes curtos e longos em 375x823 e 390x844.
- [2026-09-02] | Mensalidades reformulada como Gestão financeira | O item do menu e o título passaram a `Gestão financeira`. Foram retirados da interface os subtítulos `Acompanhamento financeiro da temporada com ações operacionais em modal` e `Prestação de contas simples: receitas e despesas com data, descrição e valor`, além do indicador superior `Pontos antecipados`; a regra de pontuação dos pagamentos permanece intacta. Atletas são exibidos como primeiro nome e inicial do sobrenome. No mobile, os sete indicadores restantes ocupam duas colunas e as tabelas principal e de caixa usam `100%`, sem mínimos de 960/760px, com filtros, células e status compactados para eliminar rolagem lateral. RCA 031 criado; build de produção e diagnósticos concluídos sem erros, com overrides confirmados no CSS gerado. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000031_gestao_financeira_mobile_com_rolagem_lateral.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar resumo, filtros, expansão de atleta e caixa em 375x823, 390x844 e desktop.
- [2026-09-02] | Barra lateral residual removida da tabela da temporada | A classificação mobile deixou de impor `min-width: 420px` e agora ocupa `100%` da largura interna do card. O override específico foi movido para a posição vencedora da cascata, com padding horizontal de 1px, posição em 28px e atleta em 96px; as seis colunas numéricas dividem o espaço restante, aproximando todo o conjunto para a esquerda. RCA 030 criado; build e diagnósticos concluídos sem erros, com regras confirmadas no CSS gerado. | frontend/src/styles.css, RCA000000000030_barra_horizontal_tabela_temporada_mobile.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar ausência de rolagem horizontal em 375x823 e 390x844.
- [2026-09-01] | Estatísticas renomeada e convertida em tabela de futebol | O item do menu passou de `Estatísticas` para `Tabela da temporada`, preservando a tipografia existente. O seletor que reordenava por estatística e toda sua lógica/CSS específicos foram removidos. A classificação agora respeita a posição oficial e exibe somente `#`, `Atleta`, `PTS`, `J`, `G`, `V`, `E` e `D`. Nomes são resumidos para primeiro nome e inicial do primeiro sobrenome, mantendo o nome completo no tooltip e o clique para perfil. A tabela foi compactada para 560px no desktop e 420px no mobile, com atleta em 108px no telefone e células mais próximas. Build de produção e diagnósticos concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Publicar o frontend e homologar a classificação com nomes simples, compostos e muitos atletas em 375x823, 390x844 e desktop.
- [2026-09-01] | Destaques mobile realinhados junto ao divisor | No breakpoint até 520px, os valores comuns passaram a usar coluna compacta de 36px e alinhamento à direita, reduzindo o espaço entre números curtos e o divisor. Aproveitamento recebeu coluna exclusiva de 62px, fonte de `.82rem` e `nowrap`, impedindo `100,0%` de avançar sobre a borda e os textos. A grade 2x2 e os layouts acima de 520px foram preservados. RCA 029 criado; build de produção e diagnósticos concluídos sem erros, com regras confirmadas no CSS gerado. | frontend/src/styles.css, RCA000000000029_alinhamento_destaques_mobile.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar em 375x823 e 390x844 com valores de um dígito e Aproveitamento em 100,0%.
- [2026-09-01] | Dupla contagem de janta eliminada | A atualização do RCA 027 aplicava a contribuição da janta depois de `onReload`, quando o agregado já continha a gravação, podendo exibir uma pessoa como duas. O delta agora é aplicado antes da recarga e a listagem do backend define o total final autoritativo. Sem selecionar `Fico para Janta?`, a contribuição permanece zero. O campo numérico foi renomeado para `Convidados adicionais`, esclarecendo que zero representa somente o usuário e um representa usuário mais um convidado. RCA 028 criado; typecheck e diagnósticos concluídos sem erros. | frontend/src/App.tsx, RCA000000000027_janta_nao_atualiza_home_apos_confirmacao.md, RCA000000000028_dupla_contagem_janta_apos_reload.md, PROJECT_CONTEXT.md | Publicar o frontend e homologar sem janta, janta sem convidado e janta com um convidado.
- [2026-09-01] | Contagem de janta atualizada imediatamente na home | O salvamento de presença passou a tipar e propagar o registro retornado pelo `PUT /matches/:id/attendance/me` até o estado global de partidas. Após a recarga, a home substitui a contribuição anterior do usuário pela nova (`1 + convidados` quando confirmou janta, zero quando removeu), evitando contagem ausente ou duplicada em alterações. O status individual também é sincronizado com a resposta persistida. RCA 027 criado; typecheck concluído sem erros. | frontend/src/App.tsx, RCA000000000027_janta_nao_atualiza_home_apos_confirmacao.md, PROJECT_CONTEXT.md | Publicar o frontend na Railway Production e homologar marcar, remover e alterar a quantidade de convidados da janta.
- [2026-09-01] | Modal automático duplicado de confirmação removido | O `GlobalAttendancePrompt`, que consultava pendências a cada 15 segundos e podia permanecer aberto por baixo do modal manual com o botão `Depois`, foi removido. A presença agora possui um único fluxo: o usuário abre pelo botão de confirmação, escolhe presença/janta, salva, os dados são atualizados e o modal fecha; alterações posteriores usam o mesmo botão e modal. Nova transcrição em `gamesPlayed` gerou 61 diagnósticos em cascata e foi restaurada, com recorrência registrada no RCA 022. RCA 026 criado; typecheck e diagnósticos concluídos sem erros. | frontend/src/App.tsx, RCA000000000022_texto_solto_apos_import_typescript.md, RCA000000000026_modal_automatico_duplicado_confirmacao.md, PROJECT_CONTEXT.md | Publicar o frontend na Railway Production e homologar que nenhum segundo modal de presença aparece após salvar.
- [2026-09-01] | Confirmação salva fecha modal e atualiza status | Os modais de confirmação abertos manualmente pela home e pela página Partidas agora recarregam os dados e fecham após salvar com sucesso; para alterar a resposta, o usuário reabre a confirmação, seleciona Confirmado, Apenas presença ou Ausente e salva novamente. Falhas mantêm o modal aberto com a mensagem da API. Uma nova transcrição dentro da propriedade `goals` de `RankingPayload` causava 106 diagnósticos em cascata; o contrato foi restaurado para `goals: number` e a recorrência foi registrada no RCA 022. RCA 025 criado; typecheck concluído sem erros. | frontend/src/App.tsx, RCA000000000022_texto_solto_apos_import_typescript.md, RCA000000000025_modal_confirmacao_permanece_aberto_apos_salvar.md, PROJECT_CONTEXT.md | Publicar o frontend na Railway Production e homologar salvamento e alteração pelos acessos da home, Partidas e prompt automático.
- [2026-09-01] | Confirmação liberada para todo usuário ativo do Ferino | O endpoint de presença, o prompt automático, a contagem de convidados e a geração de `match_players` deixaram de inferir participação pelo papel exclusivo `ATLETA`: ADMIN e COORDENADOR ativos agora também conseguem confirmar presença. O frontend captura a mensagem da API, sempre encerra o estado de salvamento, bloqueia envio duplicado e não exibe mais `Escalação salva no banco`. A corrupção acidental do nome `RankingPayload` foi restaurada e registrada no RCA 022. RCA 024 criado; frontend/backend typechecked sem erros. | backend/src/routes/matches.routes.ts, frontend/src/App.tsx, RCA000000000022_texto_solto_apos_import_typescript.md, RCA000000000024_confirmacao_403_para_admin_coordenador.md, PROJECT_CONTEXT.md | Publicar os dois serviços na Railway Production e homologar a confirmação com contas ATLETA, COORDENADOR e ADMIN ativas.
- [2026-09-01] | Tipagem de autenticação restaurada após inserção acidental | A palavra `ser` foi inserida dentro de `AuthPayload`, antes da propriedade `user`, invalidando a assinatura e gerando erros em cascata em todos os acessos a `auth.user`. O tipo foi restaurado para `{ token: string; user: User }`; RCA 022 atualizado com a recorrência e typecheck concluído sem erros. | frontend/src/App.tsx, RCA000000000022_texto_solto_apos_import_typescript.md, PROJECT_CONTEXT.md | Evitar entrada de ditado com o arquivo fonte em foco e manter `tsc --noEmit` após edições.
- [2026-09-01] | Confirmação da rodada escurecida e simplificada | O modal deixou as superfícies claras legadas e passou a usar fundo, shell, cards e controles escuros com estados semânticos preservados. Foram removidos o texto explicativo sobre situação/jantar, a faixa `Status Global / Aberto para Confirmação / Aguardando Início Oficial`, a identificação `Club no: PlayField`, o campo Observação e as instâncias de `Editar escalação` anexadas aos modais de confirmação. O checkbox visual da janta foi reduzido de 24px para 18px, mantendo o rótulo como área clicável. O salvamento agora envia `notes: null`. Durante a execução, nova transcrição foi anexada ao import de tipos; o RCA 022 recebeu registro de recorrência e o arquivo foi restaurado. RCA 023 criado; typecheck concluído sem erros. | RCA000000000022_texto_solto_apos_import_typescript.md, RCA000000000023_confirmacao_rodada_clara_e_carregada.md, frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar modal pelo prompt automático e pela home em 375x823, 390x844 e desktop.
- [2026-09-01] | Todos os atletas ativos convidados automaticamente para os jogos | A seleção manual de atletas do Ferino foi removida da criação: jogos novos, agenda manual e recorrências agora gravam automaticamente todos os usuários ativos com perfil ATLETA em `match_players`, e a confirmação de presença também reconhece atletas ativos em jogos anteriores sem depender de convocação individual. A segunda etapa da criação permanece apenas para adicionar suplentes externos opcionais, temporários naquela partida, e permite salvar sem suplente. O bloqueio visual por `isInvited` foi removido; a antiga mensagem `Você não foi convocado para este jogo` tornou-se inalcançável para atletas ativos. Dia, mês, horário, dia da semana e título do jogo foram ampliados novamente no hero. Nenhuma migration foi necessária. Frontend buildado e frontend/backend typechecked sem erros. Durante a execução, texto ditado inserido após um import causou erros em cascata; RCA 022 criado e fonte restaurada. | backend/src/routes/matches.routes.ts, frontend/src/App.tsx, frontend/src/styles.css, RCA000000000022_texto_solto_apos_import_typescript.md, PROJECT_CONTEXT.md | Homologar criação avulsa/recorrente sem suplente, com suplente externo e confirmação por atleta ativo em jogo anterior.
- [2026-09-01] | Data e ações do hero mobile ampliadas | O subtítulo relativo `Amanhã • Pré-jogo` foi ocultado somente no card verde da home, preservando essa informação na página de Partidas. Dia do mês, mês, horário, dia da semana e título foram ampliados nos breakpoints mobile; a contagem regressiva e o botão Confirmar presença também ganharam fonte e altura para aproveitar o espaço disponível. Uma anotação acidental no topo do RCA 021 foi removida sem alterar seu conteúdo técnico. Typecheck do frontend concluído sem erros. | frontend/src/styles.css, RCA000000000021_texto_solto_quebra_calculo_aproveitamento.md, PROJECT_CONTEXT.md | Homologar o hero em 375x823 e 390x844 com títulos curtos e longos.
- [2026-09-01] | Erros de compilação no cálculo de Aproveitamento corrigidos | Uma frase em linguagem natural havia sido inserida entre o `.map(...)` e o `.sort(...)` de `topEfficiency`, quebrando a sintaxe de `App.tsx` e produzindo múltiplos diagnósticos em cascata. O texto acidental foi removido sem alterar a regra de cálculo ou as mudanças recentes da home. RCA 021 criado e typecheck concluído sem erros. | RCA000000000021_texto_solto_quebra_calculo_aproveitamento.md, frontend/src/App.tsx, PROJECT_CONTEXT.md | Manter `tsc --noEmit` após edições no componente principal.
- [2026-09-01] | Percentual de Aproveitamento corrigido no mobile | O valor `100,0%` ultrapassava a largura fixa reservada aos números dos destaques e invadia a categoria Aproveitamento e o nome do atleta. Somente esse destaque recebeu uma coluna de valor mais larga e fonte percentual ligeiramente menor no mobile, preservando a grade 2x2 e os demais cards. Typecheck do frontend concluído sem erros. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar percentuais de 0,0% a 100,0% em telas mobile estreitas.
- [2026-09-01] | Hero da home ampliado e indicador de janta adicionado | O container verde do próximo jogo passou a exibir também a quantidade de pessoas confirmadas para a janta, incluindo convidados já contabilizados pelo backend em `attendanceDinnerPeople`. Os cinco indicadores agora alinham número e rótulo horizontalmente; no mobile, os quatro estados de presença permanecem em 2x2 e Janta ocupa a última linha completa. Fontes de data, título, estado, métricas e observação foram discretamente ampliadas para aproveitar o espaço recuperado. Typecheck do frontend concluído sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a home em 375x823 e 390x844 com zero e múltiplas confirmações de janta.
- [2026-09-01] | Destaques da home compactados no mobile | Os quatro indicadores de Artilheiro, Assistências, Aproveitamento e Mais cartões passaram a ocupar uma grade 2x2 no telefone. A unidade redundante exibida abaixo do número foi ocultada no mobile, mantendo categoria e atleta ao lado do valor e reduzindo a altura consumida no início da tela. Typecheck do frontend concluído sem erros. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a home em 375x823 e 390x844 com nomes curtos e longos.
- [2026-08-31] | Subtabela expandida de mensalidades integralmente alinhada ao tema escuro | O seletor claro legado da linha de detalhe alcançava também cada célula da tabela aninhada; escurecer somente a célula externa não eliminava as superfícies brancas internas. Wrapper, tabela, cabeçalho, corpo, linhas, células, alternância e hover agora têm autoridade escura explícita. RCA 020 refinada; typecheck e build de produção concluídos sem erros, com presença do seletor confirmada no CSS gerado. | RCA000000000020_subtabela_mensalidades_clara.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a expansão de atletas com cobranças pendentes e atrasadas na Railway Production.
- [2026-08-31] | Contraste do log da súmula corrigido em todas as larguras | Horários e descrições ainda mantinham cores escuras da antiga superfície branca no desktop, enquanto o painel já usava fundo verde escuro. O seletor proprietário do modal agora aplica horário verde claro e descrição quase branca em qualquer viewport. RCA 019 criada; typecheck e build de produção concluídos sem erros. | RCA000000000019_contraste_log_sumula.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o log com eventos reais em desktop e mobile na Railway Production.
- [2026-08-28] | Agenda e Prêmios passam a preencher toda a viewport mobile | A inspeção visual mostrou que os wrappers ainda encolhiam à quantidade de linhas porque a shell mobile usava altura automática e removia a referência dos `height: 100%`. As views Agenda e Prêmios receberam classes de shell próprias; apenas nelas o mobile usa `100dvh`, terceira linha `minmax(0, 1fr)` e overflow contido. Cards e wrappers tabulares agora ocupam efetivamente todo o espaço até o rodapé mesmo com poucos registros, preservando rolagem interna. RCA 018 refinada; typecheck e build de produção concluídos sem erros. | RCA000000000018_altura_tabelas_agenda_premios.md, frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar em 375x823 e 390x844 com três e muitas linhas.
- [2026-08-28] | Tabelas de Agenda e Prêmios ampliadas até o final da tela | A Agenda deixou de aplicar um segundo teto em `dvh` dentro da linha flexível e agora entrega toda a altura restante da página à tabela, preservando barras vertical e horizontal. Prêmios recebeu geometria fullscreen própria: página, card de regras e wrapper tabular crescem até o fim da área útil, neutralizando os limites genéricos de 76dvh/720px e 56dvh apenas nesse fluxo. A tabela continua com rolagem interna e os seletores finais prevalecem sobre o override mobile global. RCA 018 criada; typecheck, build de produção e diagnósticos concluídos sem erros. | RCA000000000018_altura_tabelas_agenda_premios.md, frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar Agenda e Prêmios com muitas linhas na Railway Production em desktop e mobile.
- [2026-08-28] | Súmula Inteligente centralizada na viewport | O overlay do modal recebeu a classe proprietária `draw-modal-layer` e passou a impor `place-items: center`, neutralizando o alinhamento esticado global do mobile sem afetar outros modais. A centralização horizontal e vertical preserva o limite de altura e a rolagem do conteúdo quando a Súmula Inteligente excede a viewport. Typecheck e build de produção concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000017_modal_sumula_e_rolagem_agenda.md, PROJECT_CONTEXT.md | Homologar a centralização nas etapas Detalhes e Adicionar Jogadores em desktop e mobile.
- [2026-08-28] | Modal Súmula Inteligente compactado e Agenda com rolagem completa | O modal de criação foi reduzido de 980px para até 760px, passou a acompanhar a altura real do conteúdo com limite de viewport e teve cabeçalho, card e rodapé aproximados por gaps/paddings menores. As ações permanecem em linha até 560px e empilham apenas em celulares estreitos. A página Agenda passou a reservar uma linha flexível para a tabela, cujo wrapper agora força rolagem vertical e horizontal com barras visíveis de 12px e gutter estável, neutralizando o override mobile que aplicava overflow visível. RCA 017 criada; typecheck, build de produção e diagnósticos concluídos sem erros. | RCA000000000017_modal_sumula_e_rolagem_agenda.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o modal nas etapas Detalhes/Adicionar Jogadores e a Agenda com muitas linhas/colunas na Railway Production.
- [2026-08-28] | Súmula mobile alinhada por lados, log legível e uniformes distintos | O refinamento do RCA 016 restaurou a composição solicitada: Time A à esquerda e Time B à direita em toda a escalação mobile, com divisor central visível e uma lista compacta por metade. O log operacional ganhou fonte maior, descrição quase branca e horário verde claro. No campo, os uniformes agora identificam inequivocamente as equipes: linha branca e goleiro ciano no Time A; linha vermelha e goleiro laranja no Time B. Typecheck e build de produção concluídos sem erros. | RCA000000000016_sumula_mobile_comprimida_e_sem_contraste.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar contraste, nomes longos e identificação dos quatro uniformes na Railway Production em 375x823 e 390x844.
- [2026-08-28] | Tentativa de súmula mobile empilhada supersedida | A primeira correção do RCA 016 recuperou contraste e ampliou o campo, mas empilhou Time A e Time B. Essa composição foi rejeitada e supersedida no mesmo dia pela decisão atual de manter as equipes lado a lado com divisor central. As correções de contraste e altura do campo foram preservadas. | RCA000000000016_sumula_mobile_comprimida_e_sem_contraste.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Considerar como vigente somente o registro posterior de composição alinhada por lados.
- [2026-08-28] | Confirmação, contagem e pré-jogo automatizados | O endpoint de confirmação pendente foi conectado a um modal global: cada atleta convocado pode responder pela própria conta quando a janela abre, mantendo as validações autoritativas de usuário, convocação e prazo no backend. A contagem regressiva da home ganhou atualização real a cada segundo, sem depender do polling geral. Para ADMIN/COORDENADOR, partidas DRAFT já sorteadas abrem o board operacional automaticamente a partir de 60 minutos antes do início; após fechar, a ação Reabrir súmula permanece disponível na mesma janela. RCA 015 criada. | RCA000000000015_confirmacao_contagem_e_abertura_sumula.md, frontend/src/App.tsx, PROJECT_CONTEXT.md | Homologar na Railway Production um atleta convocado e um coordenador nos limites anterior, exato e posterior a início - 60 minutos.
- [2026-08-27 19:14] | Logo do login reduzida | A logo exclusiva do painel de acesso foi reduzida de 280px para 220px no desktop e de 190px para 150px no mobile, sem alterar logos do header ou das telas de ativação. Typecheck e build de produção concluídos sem erros; dimensões computadas validadas em 1440x900 e 390x844 sem overflow horizontal. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a proporção da marca no login da Railway Production.
- [2026-08-27 19:04] | Login redesenhado e tema grafite histórico confirmado | O histórico Git e a referência visual confirmaram que o tema correto usa base grafite esverdeada `#071311`, superfícies `#0d1d1a/#132723`, acentos `#35b86b/#69d0b0`, luz verde à esquerda e mancha amarela discreta à direita; a restauração azul-marinho anterior foi supersedida. O login foi reorganizado em painel de marca e painel de acesso no desktop, com empilhamento mobile, hierarquia tipográfica, labels acessíveis, campos de 48px, CTA verde, recuperação discreta e correção de `Entrar no ferino` para `Entrar no PlayField`. A alternância de recuperação agora limpa mensagem e senha transitórias. Autenticação e endpoints não foram alterados. RCA 014 corrigida com a causa factual; typecheck, diagnóstico e build de produção concluídos sem erros. Preview validado em 1440x900 e 390x844 sem overflow ou sobreposição, incluindo a transição para recuperação de senha. | RCA000000000014_tema_escuro_divergente_e_superficies_brancas.md, frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o login e o restante do tema na Railway Production em desktop e mobile.
- [2026-08-27 18:39] | Modo escuro azul-marinho original restaurado como padrão | A direção verde-grafite registrada anteriormente foi identificada como divergente do requisito e substituída pelos valores do modo escuro histórico: fundo `#07111f`, superfícies azul-marinho, texto `#f5fbf7` e acentos azuis `#3b82f6/#93c5fd`. A camada final passou a neutralizar também seletores claros de maior especificidade em histórico financeiro, reservas e escalações da súmula, menus de evento, formulários, cards estatísticos e estados interativos. Cores próprias permanecem somente onde têm função semântica. RCA 014 criada; typecheck, diagnóstico CSS e build de produção concluídos sem erros, com cores computadas validadas no shell, header, cards e drawer. | RCA000000000014_tema_escuro_divergente_e_superficies_brancas.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar os fluxos autenticados com dados reais na Railway Production em desktop e mobile.
- [2026-08-27 18:29] | Tema escuro adotado como padrão oficial | A segunda camada de tema claro foi substituída por tokens verde-grafite escuros (`color-scheme: dark`, fundo, superfícies, bordas, textos e sombras). Uma camada final de compatibilidade eliminou superfícies brancas hardcoded em cards, cabeçalho, drawer, modais, tabelas, filtros, formulários, mensalidades, Agenda, presença, convocação, sorteio e controles operacionais. Cores claras foram preservadas somente em texto, linhas funcionais do campo e detalhes de contraste; estados de sucesso, alerta e perigo mantêm semântica própria. Typecheck, diagnóstico e build de produção concluídos sem erros. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar contraste e legibilidade do tema escuro na Railway Production em desktop e mobile.
- [2026-08-27 18:14] | Lista de convocados dividida após seis atletas | Na etapa `Adicionar Jogadores`, a lista permanece em uma coluna até seis convocados. A partir do sétimo, os atletas são separados em duas metades ordenadas e equilibradas (`ceil(n/2)` na primeira coluna e o restante na segunda), reduzindo a altura consumida no desktop; o mobile mantém uma coluna para preservar nome, posição, identificação e ação de remoção. Sorteio, confirmação e persistência não foram alterados. Typecheck, diagnósticos e build de produção concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a convocação com 6, 7 e quantidade par de atletas na Railway Production.
- [2026-08-27 18:12] | Meu perfil desktop reorganizado sem barra de rolagem | O perfil do atleta deixou de reutilizar no desktop a geometria mobile de 440px com seções empilhadas. Em telas acima de 820px, o card agora usa até 1080px e distribui identidade à esquerda, histórico na área superior direita e títulos/resumo lado a lado abaixo; `max-height` e `overflow` foram removidos exclusivamente desse modal no desktop. O comportamento mobile mantém a largura total e a rolagem protetiva. RCA 013 criada; typecheck, diagnóstico e build de produção concluídos sem erros. | RCA000000000013_barra_rolagem_meu_perfil_desktop.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o Meu perfil na Railway Production em desktop e confirmar a preservação mobile.
- [2026-08-27 18:01] | Rolagem da página de Estatísticas restrita à tabela | A `home-stack` da página de Estatísticas recebeu altura limitada à área útil e `overflow: hidden !important`, neutralizando o override mobile global que reativava rolagem no container da página. O card permanece fixo e somente `.dashboard-standings-wrap` mantém `overflow: auto`, evitando duas barras de rolagem. RCA 012 complementada; typecheck, diagnóstico e build de produção concluídos sem erros. | RCA000000000012_estatisticas_nao_aparecem_tabela.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar na Railway Production que somente a área da tabela rola em desktop e mobile.
- [2026-08-27 18:01] | Linhas da tabela de Estatísticas restauradas | A página fullscreen de Estatísticas mantinha o card com `overflow: hidden`, enquanto overrides responsivos posteriores forçavam o corpo `.dashboard-standings-wrap` para `overflow-y: visible !important`; as linhas excediam a área flexível e eram recortadas. Um override final específico da página passou a garantir altura mínima, crescimento flexível e `overflow: auto !important` em qualquer viewport. O contrato `Standing` e a consulta `/seasons/:id/standings` foram conferidos e permanecem coerentes, sem mudança de API ou banco. RCA 012 criada; typecheck, diagnósticos e build de produção concluídos sem erros. | RCA000000000012_estatisticas_nao_aparecem_tabela.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar na Railway Production a tabela com dados reais em desktop e mobile.
- [2026-08-27 17:51] | Destaques interativos, Agenda restaurada e Estatísticas ampliadas | Os cards Artilheiro, Assistências, Aproveitamento e Mais cartões passaram a preservar o `userId` do líder e abrir o modal compartilhado de perfil por clique, com semântica de botão e foco acessível. A tabela da Agenda recuperou altura própria e rolagem contida em desktop/mobile; a barra azul hardcoded foi substituída por scrollbar verde neutro. Na página de Estatísticas, o cabeçalho redundante `Estatísticas / Classificação e desempenho dos atletas na temporada` foi removido e a Tabela da temporada passou a ocupar toda a altura útil da shell, preservando o tamanho compacto da tabela exibida na home. RCA 011 criada; typecheck, diagnósticos e build de produção concluídos sem erros. | RCA000000000011_perfil_destaques_tabela_agenda.md, frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar na Railway Production o clique nos quatro destaques, a Agenda com dados reais e a altura da página de Estatísticas em desktop e mobile.
- [2026-08-27 17:34] | Partidas, Estatísticas e Agenda padronizadas como páginas | As três áreas deixaram de abrir em overlays e passaram a ser destinos reais da navegação, seguindo o padrão de cabeçalho da seção com conteúdo abaixo já usado por Mensalidades, Prêmios e Usuários. Partidas e Estatísticas reutilizam seus painéis existentes em páginas da shell; a Agenda exibe cabeçalho, ações e tabela no fluxo normal, mantendo modais somente para criar ou editar agendamentos. Estados, wrappers e estilos exclusivos dos antigos modais foram removidos. Typecheck, diagnósticos e build de produção concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar navegação e rolagem das três páginas na Railway Production em desktop e mobile.
- [2026-08-27 17:34] | Modais Partidas e Estatísticas realinhados à paleta oficial | O preto específico aplicado aos modais foi removido para evitar descaracterização. As duas visualizações mantêm a ocupação quase fullscreen em desktop e mobile, mas agora reutilizam a composição dos demais modais: overlay verde translúcido com blur, card branco translúcido, borda `var(--line)`, sombra `var(--shadow-md)` e painéis internos em `var(--surface)`. RCA 010 atualizada; typecheck, diagnóstico e build de produção concluídos sem erros. | RCA000000000010_modais_partidas_estatisticas_desktop.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar coerência visual e dimensões na Railway Production.
- [2026-08-27 17:26] | Letreiro colado à área visível da logo | A compensação horizontal do bloco `PlayField / sports & gaming hub` foi ampliada de 8px para 16px, sobrepondo a margem transparente lateral do PNG e deixando o texto efetivamente colado ao desenho. A logo manteve tamanho e posição externa. Typecheck, diagnóstico e build de produção concluídos sem erros. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o encaixe visual na Railway Production.
- [2026-08-27 17:20] | Modais Partidas e Estatísticas expandidos e escurecidos | As visualizações abertas pelo menu para `Partidas da temporada` e `Estatísticas da temporada` deixaram o limite desktop de 960px e passaram a ocupar praticamente toda a viewport, com largura e altura úteis explícitas e rolagem preservada no corpo. Overlay, card principal e cards internos agora usam fundo preto em vez do azul-marinho herdado. O comportamento quase fullscreen mobile foi preservado. RCA 010 criada; typecheck, diagnósticos e build de produção concluídos sem erros. | RCA000000000010_modais_partidas_estatisticas_desktop.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar os dois modais na Railway Production em desktop e mobile.
- [2026-08-27 17:09] | Letreiro encostado visualmente na logo | O bloco `PlayField / sports & gaming hub` foi puxado 8px para a esquerda para compensar a transparência residual e o formato curvo do símbolo, ficando visualmente colado à imagem. Tamanho e posição da logo e deslocamento externo do lockup permaneceram inalterados. Typecheck, diagnóstico e build de produção concluídos sem erros. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o encaixe final da marca na Railway Production.
- [2026-08-27 17:03] | Lockup deslocado para a direita sem alterar composição | O conjunto completo `logo + PlayField + sports & gaming hub` foi movido 10px para a direita no desktop e 8px no mobile. O tamanho da logo, a tipografia e o espaçamento interno de 3px entre símbolo e letreiro permaneceram inalterados. Typecheck, diagnóstico e build de produção concluídos sem erros. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a posição do lockup na Railway Production.
- [2026-08-27 16:56] | Espaçamento óptico do lockup reduzido | Embora o `gap` técnico já estivesse em 3px, a largura reservada pelo container do PNG mantinha uma distância visual maior entre o desenho e o letreiro. A reserva horizontal do símbolo foi reduzida em 8px no desktop e mobile, aproximando `PlayField / sports & gaming hub` sem alterar o tamanho da logo nem reativar recorte por overflow. Typecheck, diagnóstico e build de produção concluídos sem erros. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a proximidade visual final do lockup na Railway Production.
- [2026-08-27 16:49] | Logo e assinatura recompostas com espaçamento mínimo | O container do símbolo deixou de mascarar as bordas da imagem com `overflow: hidden`, eliminando o corte da antisserrilha e da sombra. `PlayField` foi restaurado acima de `sports & gaming hub`, formando um único bloco textual ao lado da logo. O espaçamento horizontal foi fixado em 3px no desktop e mobile. RCA 009 refinada; typecheck, diagnósticos e build de produção concluídos sem erros. | RCA000000000009_logo_recortada_header.md, frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o lockup final em desktop e mobile na Railway Production.
- [2026-08-27 16:38] | Lockup do header corrigido após recorte indevido | O deslocamento negativo que empurrava o símbolo PlayField para fora do header foi removido. O recorte agora atua somente sobre as margens transparentes internas do PNG, mantendo o símbolo inteiro dentro da área útil. O nome adicional `PlayField` foi retirado e `° sports & gaming hub` ficou diretamente ao lado do símbolo, centralizado verticalmente e em ciano. Estilos órfãos foram eliminados. RCA 009 criada; typecheck, diagnósticos e build de produção concluídos sem erros. | RCA000000000009_logo_recortada_header.md, frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o lockup em desktop e mobile na Railway Production.
- [2026-08-27 16:24] | Assinatura da marca adicionada ao header | O conjunto da logo PlayField foi deslocado levemente para a esquerda no desktop e mobile e passou a exibir `° sports & gaming hub` à direita do símbolo, em ciano alinhado à identidade visual da marca. A assinatura recebeu dimensão específica para telas estreitas sem alterar logos de login, recuperação ou favicon. Typecheck, diagnósticos e build de produção concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar alinhamento e legibilidade do header na Railway Production em desktop e mobile.
- [2026-08-27 16:12] | Criação separada e sorteio condicionado à confirmação | O fluxo `Criar jogo` passou a ter duas telas exclusivas: primeiro `Detalhes do Jogo`, com avanço por `Próximo`, e depois `Adicionar Jogadores`. A segunda etapa salva os selecionados como convocados ainda sem time e abre a confirmação, retirando completamente o sorteio da criação. O prompt e a gravação de presença agora aceitam somente atletas convocados; métricas de respostas usam o total convocado. Após as respostas, coordenadores podem abrir a partida, atualizar confirmações e usar `Sortear confirmados`; apenas respostas `JOGAR` entram nos times, com bloqueio equivalente no backend. Recorrências copiam a convocação para cada jogo futuro. Cancelamentos removem o rascunho abandonado. Typechecks, diagnósticos e builds de frontend/backend concluídos sem erros. | frontend/src/App.tsx, frontend/src/types.ts, frontend/src/styles.css, backend/src/routes/matches.routes.ts, PROJECT_CONTEXT.md | Homologar na Railway Production a sequência completa com atleta convocado, não convocado, ausente, só presente e confirmado para jogar.
- [2026-08-27 15:34] | Contraste de Não responderam corrigido na home | O indicador de pendências do próximo jogo deixou o amarelo claro, que conflitava com a cor branca compartilhada dos contadores, e passou a usar âmbar escuro com texto branco explícito e borda dourada discreta. A identidade semântica amarela foi preservada com leitura mais forte em desktop e mobile. RCA 008 criada; diagnósticos e build de produção concluídos sem erros. | RCA000000000008_contraste_nao_responderam_home.md, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o indicador com dados reais na Railway Production.
- [2026-08-27 15:24] | Fechamento duplicado do modal Criar jogo corrigido no mobile | O botão de fechar do modal principal fornecia `X` tanto no conteúdo JSX quanto no pseudo-elemento global `.modal-close-button::before`, ficando vulnerável à cascata responsiva. O caractere textual foi removido desse controle e uma única representação visual foi preservada pelo CSS, mantendo `aria-label`, `title` e comportamento de fechamento. RCA 007 criada; typecheck, diagnósticos e build de produção concluídos sem erros. | RCA000000000007_x_duplicado_modal_criar_jogo_mobile.md, frontend/src/App.tsx, PROJECT_CONTEXT.md | Homologar o modal Criar jogo em viewport mobile na Railway Production.
- [2026-08-27 15:14] | Resumo posicional removido da montagem do jogo | O painel visual com totais de `Elenco`, `Goleiros`, `Defesa`, `Meio` e `Ataque` foi removido para ampliar o espaço útil do modal. Permaneceram os blocos `Detalhes do Jogo`, `Adicionar Jogadores` e `Elenco Selecionado`, necessário para conferir e remover participantes; sorteio e balanceamento por posição continuam inalterados. Estilos exclusivos do painel foram eliminados. Typecheck, diagnósticos e build de produção concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o modal de montagem do jogo na Railway Production.
- [2026-08-27 15:02] | Indicador de etapas removido da montagem do jogo | O cabeçalho visual `1 Dados / 2 Participantes / 3 Sorteio` foi retirado do modal de criação e sorteio. Estados, validações, seleção de participantes, sorteio e salvamento permaneceram inalterados. Typecheck, diagnósticos e build de produção concluídos sem erros. | frontend/src/App.tsx, PROJECT_CONTEXT.md | Homologar a abertura e o fluxo completo do modal na Railway Production.
- [2026-08-27 14:52] | Menu do atleta priorizado sobre a equipe adversária | Ao abrir o menu contextual de um jogador, o painel inteiro da respectiva equipe agora recebe uma camada superior temporária (`has-open-player-menu`), eliminando o caso em que linhas do Time B cobriam o menu do Time A ou vice-versa. A correção atua no contexto de empilhamento dos painéis irmãos, enquanto o menu mantém sua camada interna elevada. Typecheck, diagnósticos e build de produção concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000005_acoes_e_layout_sumula_operacional.md, PROJECT_CONTEXT.md | Homologar a sobreposição cruzada dos menus de Time A e Time B na Railway Production.
- [2026-08-27 14:46] | Formação padrão e metade do campo protegidas | O mapa tático inicial foi refeito conforme a referência operacional, distribuindo goleiro e jogadores de linha em corredores progressivos dentro da própria metade e espelhando a formação para o Time B. O Time A passou a aceitar posições horizontais apenas entre 8% e 47%, e o Time B entre 53% e 92%, criando zona neutra junto à linha central. O mesmo limite controla arraste e renderização; coordenadas antigas são normalizadas antes do autosave. O backend passou a rejeitar qualquer atleta fixo ou convidado salvo na metade adversária. RCA 006 criada; typechecks, diagnósticos e builds de frontend/backend concluídos sem erros. | RCA000000000006_jogador_posicionado_no_campo_adversario.md, frontend/src/App.tsx, backend/src/routes/matches.routes.ts, PROJECT_CONTEXT.md | Homologar na Railway Production a formação de jogos novos, posições antigas e tentativa de arraste sobre a linha central.
- [2026-08-27 14:38] | Layout da súmula separado por viewport | A reorganização das equipes acima do campo foi restringida ao mobile até 820px, mantendo Time A e Time B lado a lado com divisor central e campo/log abaixo. No desktop, a súmula voltou à arena anterior em três colunas: Time A à esquerda, campo e log ao centro e Time B à direita, com relógio central sobre a composição. O menu contextual por atleta e o cartão azul de 2 minutos continuam em ambos. Typecheck, diagnósticos e build de produção concluídos sem erros. | frontend/src/styles.css, RCA000000000005_acoes_e_layout_sumula_operacional.md, PROJECT_CONTEXT.md | Homologar os dois layouts na Railway Production.
- [2026-08-27 14:31] | Menu de eventos da súmula reforçado | O menu contextual do atleta passou a abrir acima da linha, com fundo branco totalmente opaco, sombra forte e camadas elevadas para sobrepor escalações, campo e conteúdo inferior. A seleção agora neutraliza a opacidade herdada de atletas pendentes, eliminando o aspecto transparente. As opções de cartão deixaram de usar texto e passaram a exibir cartões retangulares amarelo, vermelho e azul; o azul mantém indicação compacta de 2 minutos. Gol e assistência também foram compactados em comandos visuais com tooltip e nome acessível. Typecheck, diagnósticos e build de produção concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, RCA000000000005_acoes_e_layout_sumula_operacional.md, PROJECT_CONTEXT.md | Homologar posição e sobreposição do menu na Railway Production em desktop e mobile.
- [2026-08-27 14:18] | Súmula operacional reorganizada | As ações permanentes de gol, assistência e cartões foram removidas das linhas do board ativo e transferidas para um menu contextual aberto por clique/teclado no atleta, com fechamento por seleção, clique externo e Esc. O menu passou a incluir cartão azul; o evento persiste o segundo exato no rascunho JSONB e retira visualmente o atleta do campo por 120 segundos de jogo, mostrando a contagem regressiva na escalação. Avatares, posições e escudos foram removidos, nomes ganharam cerca de 2px e linhas ficaram compactas. Time A e Time B agora ocupam duas metades superiores separadas por divisor central; campo e log ficam abaixo. O modal recebeu rolagem contida e listas em duas colunas no desktop. RCA 005 criada; typechecks, diagnósticos e builds de frontend/backend concluídos sem erros. | RCA000000000005_acoes_e_layout_sumula_operacional.md, frontend/src/App.tsx, frontend/src/styles.css, backend/src/routes/matches.routes.ts, PROJECT_CONTEXT.md | Homologar na Railway Production o clique nos atletas, autosave/refresh do cartão azul, retorno após 2 minutos e drag-and-drop das escalações.
- [2026-08-27 14:26] | Líder de cartões incluído nos destaques | A faixa superior de líderes da home passou de três para quatro destaques, adicionando `Mais cartões` ao lado de artilheiro, assistências e aproveitamento. O líder disciplinar é calculado explicitamente pelo maior `totalCards`, com desempate por `cardPoints` e nome, sem depender da ordenação ponderada retornada pela API; o cartão recebeu identificação visual vermelha própria. Typecheck, diagnóstico dos arquivos e build de produção concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar os quatro destaques com dados reais na Railway Production.
- [2026-08-27 14:18] | Identidade visual migrada para PlayField | O nome apresentado no frontend, planilhas e e-mails transacionais passou de POKA PRÁTIKA para PlayField. O JPEG fornecido teve o fundo quadriculado removido e foi convertido em `logo_playfield.png` com transparência real; header, login, ativação/recuperação e favicon agora usam esse ativo. O texto duplicado ao lado da marca no header foi removido e as dimensões foram adaptadas para o novo lockup. Chave de sessão e identificadores técnicos legados foram preservados para evitar logout e regressão de monitoramento. Typechecks de frontend/backend e build de produção concluídos sem erros. | frontend/public/logo_playfield.png, frontend/src/App.tsx, frontend/src/styles.css, frontend/index.html, frontend/package.json, frontend/package-lock.json, backend/src/services/mail.ts, backend/src/index.ts, PROJECT_CONTEXT.md | Homologar a nova marca na Railway Production após o deploy.
- [2026-08-27 13:42] | Cabeçalho do próximo jogo em linha única | Dia, mês, horário, dia da semana, título e estado do próximo jogo passaram a ocupar uma única faixa horizontal também no mobile; o breakpoint estreito deixou de empurrar o título para baixo e recebeu tipografia compacta, mantendo truncamento apenas no título quando o espaço for insuficiente. Typecheck, diagnóstico CSS e build de produção concluídos sem erros. | frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar o cabeçalho na Railway Production em aparelhos mobile estreitos.
- [2026-08-27 13:36] | Destaques de líderes redesenhados | A faixa de artilheiro, assistências e aproveitamento da home deixou o formato de bolhas com textos soltos e passou a usar cartões compactos com categoria, nome, valor e unidade em hierarquia própria; cada categoria recebeu cor de destaque, marcador lateral e fundo sutil, mantendo três colunas no desktop e linhas empilhadas no mobile. Typecheck e build de produção do frontend concluídos sem erros. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a home autenticada com os dados reais na Railway Production.
- [2026-08-26 18:04] | Home e menu refinados | Os atalhos `Partidas` e `Estatísticas` saíram da faixa inferior da home mobile e foram movidos para o drawer lateral junto das demais entradas; `Criar jogo` ficou no mesmo padrão de item do menu e o bloco `Temporada ativa ... OPEN` foi removido do drawer. No topo da home, os destaques de líderes perderam o card externo: agora cada item mostra só a bolinha com a estatística e o rótulo abaixo, com o nome do líder maior ao lado para consumir menos altura total. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar a navegação do drawer em perfis coordenador/admin e conferir se a home continua sem barra de rolagem no mobile.
- [2026-08-26 17:33] | Home da temporada simplificada novamente | A `Central operacional` saiu completamente da home; acima do hero verde entraram 3 bolhas com líderes de `gols`, `assistências` e `aproveitamento`, e abaixo do hero foi adicionado um card clicável de `Último jogo` que reabre a súmula confirmada para consulta. A classificação permaneceu na área inferior da home, agora em largura total. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Homologar visual do novo topo da home em desktop e mobile com uma temporada que tenha histórico confirmado.
- [2026-08-26 17:22] | Home da temporada refinada | A faixa `Temporada 2026 • OPEN` saiu da tela inicial e foi movida para dentro do drawer lateral junto com o CTA `Criar jogo`; o hero do próximo jogo perdeu o título `Central dos jogos`, removeu placar `Time A x Time B`, eliminou `% respostas`, retirou `Abrir súmula`, consolidou data/hora+título no mesmo bloco e reduziu as métricas para `Confirmados`, `Presença`, `Ausentes` e `Não responderam`, com a contagem regressiva ao lado do botão `Confirmar presença`. | frontend/src/App.tsx, frontend/src/styles.css, PROJECT_CONTEXT.md | Validar visualmente a home em desktop e mobile com dados reais da próxima rodada.
- O clique em `Finalizar jogo` agora trava localmente a súmula operacional: o cronômetro para de vez, o botão de pausar/retomar some e o board não volta a permitir retomada manual/automática enquanto a finalização estiver pendente ou em nova tentativa.
- O card `Jogos finalizados` da home foi refinado novamente para ficar mais próximo da referência visual: contador integrado ao título, confrontos em linha única com data compacta, times nas extremidades e placar central em destaque; depois, a faixa explicativa foi removida, a lista ganhou acabamento mais verde e, após ajuste de legibilidade, cada linha recuperou alguns pixels de altura. Na tabela resumida de `Mensalidades`, o subtítulo abaixo do nome do atleta também foi simplificado para remover os textos `mensalidade(s) no histórico` e `em aberto`.
- A tabela resumida de `Mensalidades` recebeu mais alguns pixels de altura por linha para alinhar visualmente com as demais tabelas administrativas, inclusive no ajuste responsivo.
- Os popovers de filtro das tabelas administrativas/financeiras foram refinados para abrir ancorados logo abaixo da respectiva coluna e em camada visual sobreposta ao restante da tabela, evitando que o menu de pesquisa fique “por trás” de headers ou linhas vizinhas.
- O modal `Agenda e confirmação dos jogos` foi expandido para comportamento fullscreen: o card principal agora ocupa praticamente toda a viewport, sem barra de rolagem no modal em si, enquanto a rolagem fica concentrada na área interna da tabela quando os dados excedem a altura/largura útil.
- Depois da primeira tentativa de fullscreen da `Agenda`, foi identificado que o override genérico `.modal-card.wide` ainda limitava esse card a aproximadamente `980px x 760px`; a correção final passou a sobrescrever explicitamente esse limite no seletor específico da Agenda para que o modal realmente ocupe a viewport.
- O frontend recebeu uma nova passada manual de refinamento mobile na home principal: header mais compacto, drawer lateral ocupando melhor a tela do telefone, seletor/status da temporada reorganizados, hero do próximo jogo com menos ruído visual (pitch mais discreto, linha `Sua resposta` removida no mobile, contadores melhor distribuídos) e `Central operacional` reorganizada para duas colunas no telefone e coluna única em telas muito estreitas.
- No mobile, a home da temporada deixou de renderizar inline os blocos pesados de `Jogos finalizados` e `Tabela da temporada`: em vez disso, a tela passou a mostrar atalhos `Partidas` e `Estatísticas`, cada um abrindo seu respectivo painel em modal dedicado. Junto disso, o container `shell-home` no telefone deixou de ficar preso em `100dvh` com `overflow: hidden`, reduzindo o efeito visual de conteúdo cortado na rolagem vertical.
- Os modais no mobile foram corrigidos globalmente para caberem dentro da viewport com rolagem própria do overlay/card, evitando corte de conteúdo; junto disso, o cabeçalho dos modais voltou a manter o botão `X` alinhado no canto superior direito em vez de herdar o empilhamento vertical do breakpoint móvel.
- No modal mobile de `Partidas`, os cards de `Jogos finalizados` foram reorganizados para leitura mais clara no telefone: cabeçalho do jogo separado do confronto, times empilhados em duas linhas estáveis e placar ocupando uma coluna fixa ao lado, reduzindo a sensação de bagunça do layout anterior.
- A barra de seleção de temporada deixou de exibir o selo separado de status (`OPEN`/equivalente) abaixo/ao lado do select, já que a troca de temporada ocorre automaticamente pelo próprio input e o status continua visível dentro das opções do seletor.
- No mobile, o cabeçalho da `Central dos jogos` passou a sobrescrever o empilhamento geral dos `card-heads`, mantendo o botão `Criar jogo` no canto superior direito do card em vez de cair para baixo à esquerda.
- No modal mobile de `Partidas`, o confronto dentro de cada card de jogo finalizado voltou a ficar em uma única linha (`Time A • placar • Time B`), substituindo a versão quebrada em duas linhas que dificultava a leitura rápida no telefone.
- Na `Central dos jogos` mobile, o hero do próximo jogo também teve o override de telas muito estreitas corrigido para manter `Time A • placar • Time B` em uma única linha, em vez de empilhar os três elementos em coluna.
- No hero mobile da `Central dos jogos`, os nomes dos times deixaram de usar truncamento com reticências no breakpoint mais estreito; quando o texto for maior, ele agora pode ocupar o espaço completo disponível sem abreviação forçada.
- O menu principal mobile teve a camada global elevada para ficar acima das grades, filtros e tabelas das telas administrativas (`Prêmios`, `Usuários` e `Config.`), evitando o sintoma de toque no botão sem aparição visível do drawer.
- O diagnóstico final do menu principal mobile mostrou que a falha nessas telas administrativas não era só de camada: no breakpoint responsivo o drawer também não estava ancorado explicitamente ao viewport. A correção final fixou o `.account-menu` com `inset: 0` no mobile, eliminando o caso em que o fundo desfoca mas o painel lateral fica preso/parcialmente fora da área visível à direita.
- As telas mobile de `Prêmios`, `Usuários` e `Config.` tiveram a contenção horizontal corrigida: a coluna principal da shell e os cards administrativos agora aceitam encolhimento com `min-width: 0`, enquanto os wrappers das tabelas ficam limitados a `100%` da viewport e concentram internamente a rolagem horizontal, sem expandir a página.
- Os badges numéricos de `Finance` e `Agenda` na `Central operacional` receberam contraste reforçado: fundo âmbar escuro, número branco, borda dourada, sombra e dois pixels extras de dimensão para permanecerem legíveis no desktop e no mobile.
- O modal `Agenda` passou a ter rolagem vertical própria e visível no desktop e mobile, mantendo também a tabela interna rolável. A política global de overlays agora bloqueia `html/body` enquanto houver `.modal` aberto e usa contenção de overscroll nos overlays/cards, impedindo que roda do mouse ou gesto touch movam a tela de fundo ao chegar ao fim de qualquer modal.
- O botão `Confirmar presença` agora permanece visível, porém apagado e desabilitado, assim que o jogo deixa o estado `DRAFT` ou o horário de fechamento da confirmação é atingido; ele não abre mais o modal de marcação nessas condições. O helper frontend também verifica `confirmationCloseAt` localmente para bloquear no horário exato, enquanto o backend preserva a validação autoritativa antes de qualquer gravação.
- A tela principal do modal `Agenda e confirmação dos jogos` foi simplificada para exibir apenas os comandos `Recorrente` e `Data específica` acima da tabela. Cada comando abre seu formulário em um modal próprio, e a ação `Editar` reutiliza o formulário de data específica. Com a retirada do formulário inline, a tabela passou a ocupar todo o espaço restante e ganhou barras horizontal/vertical visíveis e contidas também no mobile.
- O `Log da súmula` foi compactado novamente para manter visíveis aproximadamente os 3 últimos acontecimentos do jogo sem empurrar os botões operacionais; os registros antigos continuam acessíveis ao subir a rolagem invisível.
- A pausa da súmula operacional deixou de religar sozinha após refresh/polling: o board agora respeita `draftClockRunning` salvo no backend para partidas em andamento e persiste imediatamente o estado `rodando` logo após o `start`, evitando retorno automático do cronômetro e reduzindo inconsistências durante a finalização.
- O `Log da súmula` passou a manter rolagem funcional com barra invisível e a abrir ancorado no fim da lista, deixando as ações mais novas visíveis primeiro e os registros antigos acessíveis ao subir o histórico.
- O board operacional da súmula foi corrigido para usar o cronômetro local salvo em rascunho durante a partida, permitindo pausar/retomar o jogo pelo botão lateral; junto disso, o preview de gol voltou a incrementar corretamente também para o `Time B`, evitando placar parcial travado na operação ao vivo.
- O fluxo `Montar Jogo (Presença & Sorteio)` passou a aceitar `suplente convidado` antes do sorteio, com posição obrigatória e participação no mesmo balanceamento posicional dos atletas fixos; junto disso, a adição de convidado foi removida da súmula operacional após o sorteio para evitar bypass da regra.
- O `Log da súmula` ganhou exclusão direta de marcações lançadas por engano; ao remover um evento de gol ou gol contra, o placar em tela é recalculado imediatamente e o ajuste entra no histórico operacional.
- A tela de `Mensalidades` foi reestruturada para mostrar uma linha principal por atleta com resumo financeiro (`inadimplência total`, `total pago`, `último mês pago`, `próximo mês em aberto` e `meses em atraso`), deixando apenas as mensalidades abertas/atrasadas no dropdown do atleta e removendo o botão `Registrar pagamento` do header.
- O card de `Jogos finalizados` da home foi redesenhado para mostrar data e nome do jogo à esquerda e confronto completo com placar à direita; o selo/bloco de `MVP` foi removido por não agregar leitura útil.
- A moldura externa do placar na `Central dos jogos` foi removida, mantendo só o destaque do `score-pill` para liberar espaço horizontal aos nomes dos times.
- O placar da `Central dos jogos` foi compactado para não ocupar largura fixa desnecessária; `Time A`, `0 x 0` e `Time B` agora ficam visualmente mais próximos dentro do hero principal.
- A `Tabela da temporada` ativa na home ganhou um seletor de estatística no próprio card, permitindo reordenar a lista por vitórias, empates, derrotas, presenças, mensalidades, gols, assistências, cartões, saldos e aproveitamento sem sair da tela.
- O `Radar da rodada` da `Central operacional` foi removido após revisão visual; a coluna superior deixou de forçar cards com a mesma altura, evitando o branco extra abaixo da `Central dos jogos`.
- A área inferior da `Central operacional` deixou de exibir a lista de `Suspensões ativas` e passou a mostrar um `Radar da rodada`, com leitura rápida de confirmações abertas, jogos em edição e histórico confirmado da temporada.
- O sorteio automático de `Time A` e `Time B` foi endurecido para balancear por posição exata (`GO`, `ZG`, `LD`, `LE`, `MD`, `MC`, `MA`, `AT`), não só por grupos amplos; junto disso, a escalação inicial do campo passou a usar um mapa tático padrão por função, priorizando goleiro, laterais, zagueiros, meio e ataque antes de qualquer ajuste manual.
- A súmula operacional teve novo rebalanceamento vertical no desktop: a coluna central com campo + log desceu um pouco, enquanto as colunas laterais de `Time A` e `Time B` subiram mais alguns pixels para abrir ainda mais espaço visível ao banco sem mudar o alinhamento horizontal.
- Na súmula operacional, as colunas laterais de `Time A` e `Time B` foram elevadas de forma mais agressiva em direção ao card `Tempo de jogo`, preservando o alinhamento lateral e liberando mais área visível para a lista de reservas no desktop.
- O hero da `Central dos jogos` foi refinado novamente: o gramado de fundo ficou mais claro/transparente, voltou a ocupar praticamente toda a área útil do card e os nomes de `Time A`/`Time B` foram ancorados mais perto do placar para reduzir o espaçamento visual.
- As datas exibidas no frontend foram padronizadas em português no formato dia/mês/ano, substituindo saídas cruas `YYYY-MM-DD` em jogos, modais, mensalidades, caixa, exportações e temporadas por helpers centrais de formatação `pt-BR`.
- A home da temporada foi refinada novamente no topo: o card `Central dos jogos` ganhou badge de data mais larga, placar com os times visualmente mais próximos, fundo simplificado para um único gramado grande e a `Central operacional` voltou a ocupar melhor a altura disponível com a área inferior de suspensões visível.
- O fechamento da súmula no board operacional passou a congelar o cronômetro imediatamente no clique em `Finalizar jogo`, exibir `Partida finalizada` sem esperar o backend e incluir no log operacional tanto a finalização quanto as trocas manuais e automáticas.
- O autosave do board da súmula passou a normalizar `field_left/field_top` e `drawOrder` para número antes de reenviar a escalação; isso evita o erro `Expected number, received string` causado pelo retorno de colunas `NUMERIC` do PostgreSQL como texto no `pg`.
- O layout da súmula operacional no desktop foi compactado e as colunas laterais de escalação subiram alguns pixels em direção ao card do tempo, reduzindo o corte visual do banco de reservas no fim da modal.
- O gramado da súmula teve as camadas visuais (linhas, círculo e áreas) marcadas com `pointer-events: none` e os bonecos ganharam prioridade de camada, para o clique/arraste do mouse atingir diretamente o jogador em vez do overlay do campo.
- No board operacional da súmula, os bonecos dentro do campo passaram a permanecer explicitamente arrastáveis para `Time A` e `Time B` mesmo quando o atleta ainda não confirmou presença; o visual de peça “desativada” no gramado foi removido para não sugerir bloqueio de drag.
- A expansão de `Mensalidades` passou a buscar explicitamente os meses vizinhos ao mês principal aberto, por cálculo de calendário (`+1` a `+3` e `-1` a `-6`), evitando qualquer repetição do mês atual ou mistura por ordenação da lista.
- A expansão de `Mensalidades` foi corrigida para usar o `reference_month` como pivô estável do histórico do atleta, deduplicando meses repetidos e exibindo ao redor da linha principal apenas 3 meses seguintes e até 6 anteriores sem repetir o mês atual.
- A expansão de `Mensalidades` passou a montar o histórico do atleta a partir de todas as mensalidades carregadas, enquanto a linha principal continua filtrada pela temporada ativa; isso evita perder meses anteriores fora do recorte principal ao abrir o detalhe.
- A expansão de `Mensalidades` passou a listar as cobranças em torno da mensalidade principal aberta na linha: até 3 meses seguintes e até 6 meses anteriores, sem subtítulo extra na subtabela.
- A expansão por atleta em `Mensalidades` foi simplificada: o subtítulo `Outras mensalidades de ...` saiu, e a lista detalhada passou a limitar separadamente 3 próximas cobranças e 6 anteriores.
- O cabeçalho do `Log da súmula` no board operacional deixou de exibir a data da partida ao lado do título, simplificando a leitura dessa área.
- O arraste dos jogadores dentro do campo na súmula foi otimizado para mouse com preview leve em tempo real durante o movimento e persistência da posição apenas ao soltar, deixando o clique-e-arraste mais rápido e fluido.
- A tabela de `Mensalidades` foi refinada novamente: a expansão da sub-tabela agora abre ao clicar diretamente na linha principal do atleta, sem coluna ou botão dedicado de `Histórico`.
- A visão de `Mensalidades` foi corrigida após feedback: a tabela principal voltou ao formato anterior por colunas, mantendo uma linha principal por atleta/mensalidade-base e abrindo abaixo uma sub-tabela com as demais cobranças (próximas e históricas), em vez do layout em cards.
- A tela de `Mensalidades` passou a agrupar cobranças por atleta em uma única linha expansível, com filtro por atleta/mês-status, chips de mês+ano, prévia das próximas 3 mensalidades, últimas 12 e histórico completo em dropdown com ações por cobrança.
- O modal `Agenda e confirmação dos jogos` foi ampliado de forma significativa no desktop, com mais área útil, mais respiro entre formulário e tabela e layout da grade liberado para evitar que as colunas fiquem espremidas.
- A tabela de temporadas recebeu classes específicas por coluna para alinhar à esquerda os valores de ano, status, início, fim e ações, corrigindo o desencontro visual com os filtros do cabeçalho.
- Na tabela `Usuários do grupo`, a coluna `Perfil` recebeu alinhamento isolado à esquerda, sem alterar o posicionamento das demais colunas nessa iteração.
- Após revisão visual da tabela de usuários, as colunas de posição, status e ações foram realinhadas novamente para leitura à esquerda, removendo a centralização que havia piorado o encaixe visual na grade.
- A tabela de usuários recebeu alinhamento específico nas colunas de posição, status e ações, com células centralizadas de forma consistente, largura mínima própria e botões de ação sem quebra para evitar desalinhamento visual.
- O bloco de identidade do usuário no topo do drawer foi encostado no canto direito do menu, com alinhamento de texto à direita.
- O botão de fechar do drawer foi simplificado novamente para ficar apenas como um `x`, sem largura fixa ou caixa visual ao redor.
- O topo do drawer foi ajustado novamente: o `x` ganhou mais tamanho e largura própria no lado esquerdo, e o bloco `Pedro Dey / Admin` foi empurrado um pouco mais para a direita para aliviar o cabeçalho.
- O gatilho do menu hambúrguer no header foi ampliado visualmente, com ícone maior e área de respiro adicional, sem alterar o tamanho do `x` de fechamento do drawer.
- O topo do drawer lateral foi refinado: o `x` de fechar ficou maior e o bloco de identidade do usuário foi compactado lateralmente, com avatar e tipografia menores para consumir menos largura.
- O drawer lateral foi invertido para abrir do mesmo lado do hambúrguer atual; no topo do painel o bloco do usuário voltou a aparecer de forma explícita e o fechamento passou a usar apenas um `x` pequeno posicionado à esquerda.
- O header foi simplificado novamente: os ícones utilitários ao lado do hambúrguer foram removidos, e o bloco da marca POKA PRÁTIKA voltou para o lado original oposto ao menu lateral.
- O botão `X` de fechar do drawer lateral foi desacoplado do bloco de perfil e fixado no canto superior direito do menu, deixando o topo do painel mais limpo.
- O desfoque do menu lateral foi reforçado no nível do `shell`: ao abrir o hambúrguer, o drawer sobe como camada separada e o restante da aplicação recebe blur real de fundo, além do backdrop desfocado.
- O drawer do menu hambúrguer foi ampliado: agora é ancorado na viewport inteira (`fixed`), ocupa 100% da altura da tela e recebeu largura maior no desktop, mantendo uma largura adaptada no mobile.
- O antigo dropdown do header foi substituído por um menu hambúrguer lateral: ao abrir, um drawer animado entra pela borda esquerda do projeto com backdrop desfocado sobre o restante da interface, mantendo no painel o perfil do usuário e as mesmas ações de navegação/logout.
- O header principal foi refinado novamente: menu admin/perfil e ícones utilitários passaram a formar um bloco único na esquerda, separados por uma linha vertical sutil; os ícones perderam os antigos containers e ficaram visuais limpos, apenas com o glyph.
- O header principal foi invertido no desktop: menu da conta/admin e grupo de ícones utilitários foram deslocados para a esquerda, enquanto o bloco da marca POKA PRÁTIKA passou para a direita, com o dropdown do menu reancorado para abrir corretamente desse novo lado.
- A remoção da barra de rolagem da página inicial foi reforçada no container raiz da aplicação: a view `temporada` agora marca o `shell` principal e bloqueia o scroll global da home diretamente nesse nível, mantendo o scroll apenas nas áreas internas necessárias.
- O componente compartilhado `TableFilterHeader` foi padronizado para renderizar o ícone de filtro à esquerda do rótulo, alinhando agenda, usuários, temporadas, pontuação e demais tabelas administrativas ao mesmo layout visual.
- Os cabeçalhos com filtro das tabelas foram refinados novamente para começar na borda esquerda da célula, com o ícone e o nome da coluna encostados no início visual da tabela.
- Os cabeçalhos filtráveis das tabelas foram ajustados para mostrar o ícone de filtro colado à esquerda do texto da coluna em todo o frontend administrativo/financeiro.
- A home da temporada passou a bloquear a rolagem do container inteiro (`home-stack dashboard-main`), preservando o scroll apenas nas áreas internas como a tabela/classificação.
- As exportações do frontend deixaram de gerar CSV cru e passaram a baixar planilhas `.xlsx` com `exceljs`, cabeçalho tematizado POKA PRÁTIKA, autofiltro, colunas ajustadas e formatação automática para moeda, mês, datas e status financeiros.
- A súmula operacional agora preserva trocas manuais também antes do início oficial enquanto o autosave consolida a edição, mantém `startsOnBench` salvo ao reconstruir a escalação e preserva o histórico local da rotação automática durante o refresh periódico; além disso, a normalização da escalação passou a redistribuir goleiros naturais entre os times quando uma recomposição deixa os dois no mesmo lado, e o backend passou a reconstruir o estado operacional persistido antes do `start` (papel de goleiro + quem começa em quadra/banco pela ordem salva) e a validar goleiro/linhas apenas entre os titulares em quadra, sem contar o banco.
- O container do frontend passou a usar um `nginx.conf` principal próprio (`frontend/nginx-main.conf`) com `master_process off`, `worker_processes 1` e `error_log /dev/stderr warn`, eliminando os logs `start worker process`/`notice` que a Railway estava destacando como erro mesmo sem falha real da aplicação.
- A escalação salva pela edição operacional e pelo board da súmula passou a marcar como presentes todos os atletas mantidos em `Time A`, `Time B` ou `Só presença`, para que o `start` siga a escalação escolhida pela coordenação e não bloqueie por `present=false` herdado de confirmações antigas/pendentes.
- A importação do Excel em `config.` foi redesenhada: o modal agora mantém a revisão aberta após importar, exibe preview estruturado das linhas coladas, resumo visual da carga e tabelas separadas para registros importados e linhas que exigem revisão, deixando o fluxo bem mais legível.
- A home da temporada teve a barra de rolagem externa removida no container principal (`dashboard-main`); o layout agora mantém o scroll apenas nas listas e tabelas internas quando necessário, deixando a tela inicial mais limpa.
- A súmula operacional agora trata a troca manual entre titular e banco transferindo também o papel de goleiro quando necessário, prioriza um goleiro operacional entre os titulares em campo e ganhou fallback de arraste por `pointermove/pointerup` global para tornar o reposicionamento das peças no gramado mais confiável.
- O arraste das peças dentro do campo na súmula foi refeito com `pointer capture` no próprio quadradinho do atleta, corrigindo o fluxo de clicar, segurar e arrastar com mouse para reposicionar número e nome no gramado.
- O backend teve a tipagem do executor SQL na rota de súmulas alinhada com `pg.QueryResult`, corrigindo o erro de build TypeScript gerado após o suporte a convidados e posicionamento manual.
- Na súmula operacional, uma troca manual entre titular e banco agora pausa a rotação automática do respectivo time para não desfazer a substituição escolhida; além disso, a linha visual do atleta foi simplificada para mostrar só posição sob número/nome, sem os textos auxiliares de não confirmado e arraste.
- A súmula operacional agora permite arrastar as bolinhas dentro do campo para definir posição manual por atleta, persistindo `field_left/field_top` em `match_players` via migration `18_posicionamento_manual_sumula.sql`; junto disso, o modal da súmula foi ampliado e deixou de usar rolagem interna no desktop.
- O modal da súmula operacional foi ampliado e reorganizado: o tempo de jogo ficou centralizado no topo, o log passou a ficar logo abaixo do campo na coluna central, os botões de iniciar/finalizar foram empilhados abaixo do log e o suplente convidado saiu do topo para um botão que abre modal próprio.
- A súmula operacional agora aceita suplente/convidado temporário sem criar atleta fixo: o board passou a adicionar/remover convidado por nome/posição/time, o backend persiste convidados e eventos por identidade própria da partida, e a migration manual `17_convidados_temporarios_sumula.sql` libera esse modelo no PostgreSQL.
- A criação/gerenciamento da partida foi endurecida em três pontos: a escalação agora nasce da lista completa de atletas cadastrados, ordenada estritamente por posição original; confirmações não promovem ninguém a goleiro por padrão; e o plano automático de trocas passou a usar janelas fixas calculadas pelo tempo total da partida e pela sequência numérica dos jogadores, buscando minutagem igual entre os jogadores de linha presentes.
- A modal principal de `Criar jogo` passou a aceitar `Jogo recorrente`: checkbox no passo de dados, frequência semanal, escolha explícita do dia da semana e data final; ao salvar a súmula do jogo base, o sistema agenda automaticamente os próximos jogos do período usando a recorrência existente do backend.
- A tabela de `Usuários` teve o alinhamento das colunas textuais ajustado: nome e e-mail agora ficam explicitamente à esquerda na grade administrativa, evitando aparência centralizada fora do respectivo cabeçalho.
- O servico de e-mail foi redesenhado: convites de ativacao e recuperacao de senha agora usam um template HTML mais apresentavel, com hero visual, CTA destacado, bloco de instrucoes e fallback seguro quando a URL do frontend nao estiver disponivel.
- O `config.` agora exibe `Temporadas`, `Usuários` e `Pontuação` em tabelas com filtros por coluna; a Central de `Prêmios` também ganhou filtros laterais em cada coluna da tabela editável.
- O modal `Agenda` foi ampliado e a lista de jogos pré-definidos virou tabela com filtros por coluna, exibindo data, jogo, horário, confirmação, janela e presenças de forma mais legível.
- O modal `Agenda` foi alargado novamente e a tabela interna passou a ocupar 100% da largura útil, removendo a necessidade de barra de rolagem lateral em desktop.
- O modal `Perfil do atleta` foi redesenhado para um card vertical inspirado no layout visual de referência, com avatar central grande, ações logo abaixo, histórico visual de temporadas e cards compactos para títulos/badges e resumo da carreira.
- O menu da conta foi simplificado: `Trocar senha` saiu do dropdown e ficou disponível apenas dentro do modal `Perfil do atleta`, evitando ação duplicada.
- Os filtros das tabelas foram simplificados para busca direta por texto no popover; a lista grande de opções por coluna foi removida para manter a filtragem mais rápida e limpa.
- A nova súmula operacional foi simplificada: banco de reservas visível para os dois times, troca manual por botão removida em favor de arrastar e soltar, autosave do quadro sem botão explícito e fluxo reduzido a `Iniciar jogo`, `Finalizar jogo` e `Log da súmula`.
- A súmula operacional agora reaplica automaticamente o roteiro de trocas por tempo de jogo com base nas regras legadas do projeto, permite rearranjo manual entre os dois times por drag-and-drop e deixa atletas ainda não confirmados visualmente apagados em campo, titulares e bancos.
- A súmula aberta passa a se atualizar sozinha enquanto estiver em edição para refletir novas confirmações; atleta acende ao confirmar presença para jogar ou apenas comparecer, e continua apagado só quando ainda não respondeu ou marcou ausência.
- O mobile recebeu um override responsivo global: telas principais agora empilham os blocos em coluna única, os elementos foram reduzidos, áreas com `max-height`/rolagem interna perderam scroll no celular e tabelas/modais foram comprimidos para evitar barra lateral sempre que possível.
- O ajuste mobile das tabelas foi recalibrado: em vez de esmagar colunas e quebrar cabeçalhos, o celular agora preserva largura mínima e legibilidade nas tabelas, usando rolagem horizontal apenas dentro dos blocos tabulares mais largos.
- A súmula operacional deixou de exibir os blocos separados de roteiro/trocas por time; além disso, a montagem local dos jogadores agora mescla atletas confirmados convidados com a escalação já salva e parou de cortar titulares, reservas e bolinhas do campo por limite fixo de `slice`.
- A súmula operacional agora bloqueia gol, assistência e cartões até o clique em `Iniciar jogo`, sincroniza imediatamente o cronômetro com o `started_at` oficial devolvido pelo backend e mantém o ajuste de posições por drag-and-drop enquanto a súmula ainda não foi confirmada.
- O clique em `Iniciar jogo` agora coloca a súmula imediatamente em modo operacional também no frontend: o cronômetro começa na hora, a rotação automática por minuto passa a valer sem esperar novo fetch do status e o botão de finalizar só aparece depois desse início local/oficial.
- A súmula operacional passou a normalizar automaticamente a escalação antes de salvar/iniciar: cada time fica com exatamente 1 goleiro escolhido pela função/posição disponível, e trocas por arraste entre lados reaplicam esse ajuste para evitar erro `O time A/B precisa ter exatamente 1 goleiro` no `start`.
- A aba `Usuários` passou a usar uma versão tabelada própria com colunas de nome, e-mail, perfil, posição, status, ações e retorno; a central de `Prêmios` também foi migrada para tabela editável de regras.
# PROJECT_CONTEXT — POKA PRÁTIKA

Data: 2026-07-23

## Estado atual

Foi criada a base full-stack do sistema POKA PRÁTIKA, seguindo o padrão TOIT/Railway:

- Repositório único com diretórios separados `frontend` e `backend`.
- Backend Node.js/TypeScript com Express e PostgreSQL via `pg`, sem ORM.
- Frontend React/Vite/TypeScript/Tailwind, mobile-first e interface compacta.
- Frontend em produção usa Nginx com `docker-entrypoint.sh` para gerar `/runtime-config.js` a partir de `VITE_API_URL` no runtime Railway, evitando tela branca quando a variável existe no serviço mas não entrou no build Vite.
- Identidade visual original criada em `frontend/src/assets/poka-pratika-logo.svg`, com tom cômico de futebol amador/perna de pau, referência a Balneário Camboriú/SC e paleta azul média aplicada ao escudo e aos elementos de destaque do sistema; no símbolo, `POKA` e `PRÁTIKA` usam o mesmo tamanho de fonte, ambos ampliados em 15% sobre o tamanho anterior de `POKA`, sem a antiga bolinha amarela e com os textos 2px mais baixos.
- Migrações SQL manuais em `migrations/01_core_schema.sql`, `migrations/02_pagamentos_vencimento_pontuacao.sql`, `migrations/03_saldo_inicial_temporada_excel.sql`, `migrations/04_posicoes_oficiais_atletas.sql`, `migrations/05_sumula_rascunho_operacional_autosave.sql`, `migrations/06_selecao_do_ano_7_votos.sql`, `migrations/07_eventos_gol_contra.sql`, `migrations/08_email_case_insensitive_unico.sql`, `migrations/09_reparo_schema_sumula_operacional.sql` e `migrations/10_correcoes_auditadas_sumula.sql`.
- Sem criação de `.env` e sem hardcode de credenciais/URLs.
- Backend valida obrigatoriamente `NODE_ENV=production`, `PORT=8080`, `DATABASE_URL`, `JWT_SECRET`, `ALLOWED_ORIGINS` e `FRONTEND_URL` no startup Railway.
- Backend expõe `/health` e `/ready`; `/ready` consulta o PostgreSQL com SQL nativo para homologar conexão real do serviço.
- CSS customizado foi mantido apenas como complemento ao Tailwind para ajustes finos de densidade visual e responsividade; modais longas e telas pequenas agora priorizam rolagem segura para não cortar formulários operacionais.
- Homologação final documentada em `docs/homologacao-final.md`; garantia técnica consolidada em `docs/garantia-qualidade.md`; troubleshooting Railway em `docs/troubleshooting-railway.md`; o sistema só deve ser considerado finalizado após execução dos fluxos de aceite na Railway.

## Funcionalidades implementadas

### Banco

- Usuários com perfis `ADMIN`, `COORDENADOR`, `ATLETA`.
- Usuários/atletas com posição cadastral oficial em `users.position`: `GO`, `ZG`, `LD`, `LE`, `MD`, `MC`, `MA` ou `AT`.
- A posição cadastral do atleta é independente do papel operacional da súmula; `match_players.role_in_match` permanece com `GOLEIRO`, `LINHA` e `PRESENTE_SEM_JOGAR`.
- Temporadas com status `DRAFT`, `OPEN`, `CLOSED`.
- Súmulas, jogadores da súmula, eventos, pagamentos, pontuação, premiações, votação, badges e suspensões.
- Saldos iniciais importados da tabela atual do Excel em `season_standing_adjustments`, somados às novas súmulas confirmadas.
- `season_standings` separa `games_played` (jogos efetivamente jogados) de `presences` (compareceu, mas não jogou), conforme a planilha real da temporada 2026.
- Métricas da classificação geral incluem gols da equipe pró/contra/saldo, aproveitamento por atleta, médias de gols e cartões ponderados.
- View `season_standings` para pontos corridos/rankings.
- Índices para temporadas abertas, súmulas, eventos, pagamentos, votos e suspensões.

### Backend

- Autenticação JWT e senha com bcrypt.
- Login oficial por e-mail e senha.
- Bootstrap seguro do primeiro admin quando o banco ainda não tem usuários.
- Usuários autenticados podem trocar a própria senha em `/auth/change-password`, informando senha atual e nova senha; o backend valida bcrypt, impede reutilizar a senha atual, atualiza `password_hash` e invalida tokens pendentes de recuperação/ativação.
- Cadastro de usuários por ADMIN/COORDENADOR pode ser feito sem senha inicial; nesse caso o sistema gera token seguro e envia e-mail de ativação pelo Microsoft Graph.
- E-mail de ativação usa o assunto `POKA PRÁTIKA: ATIVE SEU CADASTRO`; recuperação usa `POKA PRÁTIKA: ALTERE SUA SENHA`.
- CRUD base de usuários pelo ADMIN; COORDENADOR pode cadastrar atletas, mas não cria ADMIN/COORDENADOR.
- Gestão administrativa de usuários foi reforçada com edição de nome/e-mail/papel/posição/status, proteção contra remoção do último ADMIN ativo, redefinição de senha pelo ADMIN e reenvio de convite de ativação por Microsoft Graph.
- Configuração de pontuação pelo ADMIN e COORDENADOR.
- Configuração de categorias de premiação pelo ADMIN em `/settings/awards`, permitindo alterar rótulos e ligar/desligar categorias votáveis sem mudar código.
- Temporadas: criar, iniciar, encerrar, ranking e classificação.
- Encerramento de temporada gera prêmios/badges automáticos de ranking para alimentar a carreira histórica dos atletas.
- Endpoint de carreira do atleta consolida estatísticas, temporadas, títulos, badges e suspensões.
- Súmulas: criar, iniciar, submeter, confirmar, registrar eventos e cálculo de trocas.
- Detalhe de súmula `DRAFT` vazia foi endurecido para abrir sem atletas/eventos, sem depender de `scheduled_start/scheduled_end` e sem quebrar caso `match_corrections` ainda não exista; bancos existentes devem executar a migration `09` para autosave operacional e a migration `10` para histórico auditável de correções.
- O `GET /matches/:id` teve o cálculo de `availableMinutes` reescrito para sintaxe PostgreSQL explícita (`EXTRACT(EPOCH FROM (...)) / 60`), corrigindo o erro Railway `syntax error at or near "AS"` que retornava `500` ao abrir a súmula.
- Súmulas existentes em `DRAFT`, `RUNNING` ou `SUBMITTED` podem ter árbitro, data, times e escalação reabertos e editados pela interface, persistindo em `/matches/:id/lineup` e recalculando roteiro de trocas após salvar.
- Início oficial da partida pelo botão `Jogo iniciado`, persistindo `started_at` com o instante real do clique no PostgreSQL e exibindo em horário de Brasília.
- Tempo de jogo e cadência de substituições respeitam a janela fixa da quadra: aluguel das 20:00 às 21:00, mas se o jogo iniciar atrasado o tempo útil passa a ser apenas o intervalo entre `started_at` e 21:00.
- Rascunho operacional da súmula persiste `draft_team_a_score`, `draft_team_b_score`, `draft_events`, `draft_clock_seconds`, `draft_clock_running`, `draft_saved_by` e `draft_saved_at`, evitando perda de placar/eventos se o dispositivo desligar antes da submissão.
- Súmulas não confirmadas podem ser canceladas por ADMIN/COORDENADOR sem pontuar a temporada.
- Detalhe da súmula retorna o histórico de correções auditadas com antes/depois, motivo, responsável e data.
- Súmulas validam consistência antes de pontuar: só confirmam após submissão, eventos precisam ser de atletas escalados e gols lançados precisam bater com o placar por time.
- Súmulas não podem ser submetidas ou confirmadas sem `started_at`; isso bloqueia bypass de pontuação sem o botão `Jogo iniciado`.
- Eventos relacionados são validados para impedir vínculo com atleta fora da súmula, presente sem jogar, atleta do outro time ou o próprio autor do evento.
- Frontend do editor de eventos deriva o time pelo atleta selecionado e filtra atletas relacionados para o mesmo time, evitando seleção visual contraditória.
- Súmulas confirmadas podem ser corrigidas por ADMIN/COORDENADOR através de correção auditada com motivo obrigatório, gravando antes/depois em `match_corrections`, criada pela migration `10_correcoes_auditadas_sumula.sql`.
- Eventos oficiais incluem `GOL_CONTRA`, além de gol, assistência e cartões amarelo/vermelho/azul.
- `GOL_CONTRA` foi alinhado também no SQL base e na migração incremental `07_eventos_gol_contra.sql`, evitando falha em bancos novos ou já existentes.
- Importação de saldo inicial da tabela do Excel por temporada, para iniciar a continuidade da temporada 2026 exatamente na classificação atual.
- Pagamentos: controle de mensalidades por ADMIN/COORDENADOR e visão própria para atleta.
- Mensalidades possuem vencimento; pagamento registrado antes do vencimento gera 1 ponto na temporada vinculada.
- Mensalidades agora têm resumo financeiro por temporada, geração mensal em lote para todos os atletas ativos, preservação de pagamentos já quitados e observações operacionais.
- Votação de premiações com resultado restrito ao ADMIN.
- Seleção do ano possui votação estruturada com 7 votos por atleta: 1 goleiro (`GO`) e 6 jogadores de linha, persistidos em `award_votes.vote_slot`.
- Consolidação de vencedores votados pelo ADMIN grava prêmios no histórico e badges dos atletas.
- Suspensões automáticas por cartão vermelho ou 3 amarelos confirmados na temporada.
- Baixa de suspensão exige partida confirmada, posterior ao jogo gatilho e vinculada à mesma temporada quando houver `season_id`.
- Lista de usuários para atleta autenticado é reduzida a dados públicos de usuários ativos; e-mail/status completos ficam restritos a ADMIN/COORDENADOR.
- E-mails de usuários são normalizados para minúsculas, recebem índice único case-insensitive na migração `08` e conflitos de duplicidade retornam `409` sem vazar detalhes internos.
- Integração de recuperação de senha preparada para Microsoft Graph.

### Frontend

- Login e recuperação de senha; a opção visual `Primeiro acesso` foi removida da página de login para simplificar a experiência pública.
- Inputs de senha usam `autocomplete` adequado (`current-password` e `new-password`), removendo o aviso do console do Chrome sem alterar regra de autenticação.
- Login e header com logo/microcopy do POKA PRÁTIKA de Balneário Camboriú/SC.
- Header autenticado expõe `Trocar senha` para ADMIN, COORDENADOR e ATLETA, abrindo modal responsiva com senha atual, nova senha e confirmação, chamando o backend real.
- Header autenticado permite abrir o próprio perfil clicando no avatar/nome; perfis de terceiros são visualizados no mesmo modal sem edição de dados de outro usuário.
- Dashboard de temporada, pontos corridos, rankings e suspensões.
- Dashboard com pódio visual, KPIs compactos, rankings e estados vazios amigáveis.
- Dashboard exibe classificação alinhada à planilha real: pontos, jogos, vitórias, empates, derrotas, presença sem jogar, mensalidade, gols da equipe, saldo e aproveitamento.
- Rankings contemplam artilharia com gols contra/saldo/média, assistência com média, assiduidade com jogos+presença e cartões por pontos/total/média.
- A aba `perfis` foi removida; carreira acumulada de atleta/usuário agora abre em modal sobre fundo fumê transparente, acionado pelo avatar/nome do usuário no header ou por nomes clicáveis na classificação/rankings.
- Lista de súmulas, criação com atletas por time/presença, ordem de sorteio, sequência, banco, cronômetro digital e fechamento por eventos.
- Criação de súmula operacional com busca por nome/e-mail a partir de 3 caracteres, inclusão rápida no Time A, Time B ou presente sem jogar, e ordenação drag-and-drop da sequência de substituições.
- Criação de súmula operacional permite montar uma lista de presença e acionar o balanceamento automático por posições, distribuindo `GO`, defesa/laterais, meio-campo e ataque entre Time A e Time B com diferença mínima por grupo.
- A tela `Nova súmula` agora cria imediatamente uma súmula `DRAFT` no banco e autosalva a escalação via `/matches/:id/lineup`, protegendo a montagem de presentes/times/banco/sequência antes do fechamento da modal.
- O detalhe da súmula permite reabrir e editar escalação existente em `DRAFT`, `RUNNING` e `SUBMITTED`, incluindo árbitro, data, times, banco, goleiro/linha, presentes, rebalanceamento e salvamento real no backend.
- Backend valida edição de escalação contra atletas duplicados/inativos e contra eventos já lançados, além de bloquear `Jogo iniciado` sem exatamente 1 goleiro e pelo menos 6 linhas em cada time.
- O balanceador define o primeiro `GO` de cada time como `GOLEIRO`, transforma excedentes em `LINHA` para evitar múltiplos goleiros fixos no mesmo time e marca automaticamente como banco os jogadores de linha acima dos 6 primeiros de cada equipe.
- Cronômetro oficial é derivado do `started_at` persistido, não do estado local do celular; ao reabrir a súmula, o tempo continua como se o dispositivo nunca tivesse desligado.
- Editor de placar/eventos usa autosave no endpoint protegido `/matches/:id/draft` enquanto a súmula não está confirmada.
- Súmulas da aba são filtradas pela temporada selecionada.
- Operação de jogo agora expõe início oficial da súmula, cancelamento seguro de súmulas não confirmadas, submissão, confirmação e correção auditada.
- Cancelamento de súmula usa confirmação inline na interface, sem `window.confirm`, para UX mais consistente em celular.
- Histórico de correções auditadas aparece no detalhe da súmula, evitando ajuste invisível de placar/eventos.
- Modal operacional da súmula ganhou lançamento rápido por atleta: botões `G`, `A`, `CA`, `AZ`, `CV` e `GC` por jogador de cada time; `GC` mantém a regra de gol contra pontuar o adversário sem somar para artilharia do autor.
- A criação de partida na temporada usa CTA claro `Criar jogo`; a lista de jogos ficou na home da temporada ativa como entrada visual discreta para abrir a súmula em modal fumê.
- Exibição automática de roteiro de trocas conforme súmula tradicional.
- Gestão de mensalidade com mês de referência, vencimento, data de pagamento, status e indicação de ponto antecipado.
- Aba de mensalidades exibe KPIs financeiros, lista densa de cobranças, exportação CSV e ações operacionais em modais fumê: `Gerar mês` para atletas ativos e `Registrar pagamento` individual.
- Votação de premiações sem `Vera Verão`, com apuração ADMIN e consolidação de winners/badges; `SELECAO_ANO` exibe formulário especial com 1 goleiro + 6 linhas.
- Configurações de usuários e pontuação para ADMIN/COORDENADOR, com criação de perfis privilegiados restrita ao ADMIN.
- ADMIN consegue editar cadastro completo de usuários, redefinir senha e reenviar convite de ativação pela interface; a edição de usuário agora abre em modal individual para não poluir a lista operacional.
- ADMIN configura categorias de premiação na aba `premios` através de modal fumê, controlando nomes e votação habilitada sem poluir a tela principal de votação/apuração.
- Classificação geral e mensalidades têm exportação CSV operacional.
- Painel administrativo `config.` foi reorganizado como home operacional com cards de ação e modais fumê para criar temporada, editar pontuação, criar usuário e importar Excel, mantendo listas densas de temporadas e usuários na tela principal.
- ADMIN pode editar perfil, posição oficial e status ativo/inativo de usuários; COORDENADOR mantém criação operacional de atletas sem elevação indevida de privilégio.
- Suspensões abertas podem ser marcadas como cumpridas na temporada a partir de uma partida confirmada.
- Painel administrativo permite colar a tabela atual do Excel com cabeçalho e importar o saldo inicial da temporada, retornando linhas importadas e linhas ignoradas para revisão.
- O formulário antigo de criação de súmula foi removido; existe apenas o fluxo operacional com busca e drag-and-drop.
- UI refinada com logo original azul, palavra `PRÁTIKA` abaixo de `POKA` no símbolo, pódios, cards, microcopy cômica, modais roláveis, listas suspensas com opções em fonte preta e layout compacto/mobile-first com line-height global reduzido em 10%.
- A navegação avançou para o padrão painel principal + modais fumê em todos os módulos principais: jogos/súmulas aparecem como relatório na temporada ativa, operação da partida abre em modal amplo, mensalidades usam modais para geração/registro, prêmios usam modal para configuração administrativa e `config.` usa modais para ações sensíveis.
- O pop-up global automático de confirmação de presença foi removido; atletas agora confirmam presença apenas pelo botão do card do jogo e pela modal do próprio jogo, sem interrupção recorrente ao abrir a home.
- O dashboard principal foi redesenhado em tema light/vibrante com header fixo, seletor de temporada no topo, card hero do próximo jogo com textura de gramado e contagem regressiva, widget operacional lateral com disciplina/financeiro/quadra e seção inferior unificada com jogos finalizados horizontais, líderes, abas e paginação de estatísticas.
- O dashboard principal foi refinado para ficar aderente à referência visual: a temporada voltou para a faixa abaixo do header, o topo ganhou ícones de utilitários, o próximo jogo passou a usar badge de data à esquerda com miolo em gramado e trilha lateral de status/ações, o painel operacional foi compactado em 3 widgets e a base foi reorganizada em duas colunas com jogos finalizados à esquerda e tabela de atletas à direita.
- O card `Central dos jogos` passou a usar a biblioteca `react-soccer-lineup` como camada real de fundo do campo no hero do próximo jogo, com overlay escuro para preservar legibilidade dos dados operacionais.
- O dashboard principal recebeu novo refinamento para aderir ainda mais à imagem de referência: painel operacional lateral simplificado em três widgets compactos, rótulos do hero ajustados para `Não responderam`/`Para ajustar` quando aplicável e densidade visual mais próxima da composição alvo.
- O dashboard principal foi expandido para aproveitar mais a largura útil da tela, com colunas superiores/inferiores recalibradas e o `Central dos jogos` escurecido em verde mais fechado, mantendo apenas um campo grande no fundo em vez do efeito visual de múltiplos campos.
- O topo do dashboard foi refinado novamente para evitar cortes no `Central dos jogos`: a coluna lateral do hero foi alargada para comportar `Fechado para Confirmação`, `% respostas` e os botões completos, enquanto o `Central operacional` ganhou mais largura útil e cards mais altos para impedir sobreposição entre `Suspenses`, `Finance` e `Agenda`.
- Todos os fechamentos de modal foram padronizados visualmente para botões `X`, substituindo os antigos CTAs textuais `Fechar` nos headers e áreas de topo dos modais.
- A modal `Súmula Inteligente` de criação de jogo foi redesenhada para o layout claro em wizard da referência, com header compacto, stepper horizontal, card esquerdo de detalhes, coluna direita de busca/sorteio e footer operacional fixo com `Cancelar` e `Salvar súmula final`.
- A modal `Súmula Inteligente` recebeu refinamento visual adicional para aderir melhor à referência: ícones semânticos nos títulos/indicadores, botão de sorteio com dois dados, camisas distintas para `Time A` e `Time B` e densidade reduzida para evitar barra de rolagem no container principal do modal.
- Os ícones da modal `Súmula Inteligente` foram ampliados levemente para melhorar a leitura visual sem reabrir o problema de densidade/rolagem do modal.
- O stepper da modal `Súmula Inteligente` foi ajustado para o padrão em pílulas conectadas da referência, com trilha ao fundo e sem a linha atravessar visualmente a pílula central.
- O stepper da modal `Súmula Inteligente` foi refinado novamente para que cada pílula envolva número + texto no mesmo bloco visual, impedindo a trilha de passar sobre o rótulo do passo central.
- A lista de atletas selecionados na modal `Súmula Inteligente` foi compactada para linhas únicas com nome + posição e remoção discreta, evitando cards grandes quando houver muitos participantes.
- A lista de atletas selecionados na modal `Súmula Inteligente` agora quebra em duas colunas quando ultrapassa 6 participantes, reduzindo a altura do modal sem perder legibilidade.
- A lista de atletas selecionados na modal `Súmula Inteligente` foi simplificada novamente para permanecer sempre em duas colunas no desktop, mantendo a altura mais controlada desde poucos até muitos participantes.
- A lista de atletas selecionados na modal `Súmula Inteligente` passou a exibir apenas o primeiro nome de cada atleta, reduzindo ainda mais a largura das linhas compactas em duas colunas.
- O resultado do sorteio na modal `Súmula Inteligente` foi compactado para linhas pequenas com primeiro nome + posição; o clique na linha agora alterna se o atleta começa no banco, enquanto a definição fina de função operacional fica para depois.
- O resultado do sorteio foi removido do corpo principal da modal `Súmula Inteligente`: ao clicar em `Sortear times`, abre uma confirmação rápida separada com `Time A` e `Time B`, onde o clique na linha do atleta alterna banco antes do `Salvar súmula final` no modal principal.
- A confirmação rápida de `Time A` e `Time B` foi recentralizada verticalmente no meio da tela, deixando de ficar presa visualmente na parte superior do overlay.
- A `Confirmação da rodada` foi redesenhada para seguir a referência visual clara com ícones de status, dashboard de escolha única, bloco lateral de janta/observação e card de `Sua Resposta Recente`, substituindo a versão antiga mais genérica.
- Durante esse redesign foi necessário reparar a estrutura do `frontend/src/App.tsx`, removendo uma cópia indevida da `AttendancePanel` dentro do editor de escalação, reconstruindo a cauda funcional do `ExistingLineupEditor` e restaurando o componente `SubstitutionManager` para manter o build íntegro.
- Os ícones de bola e refeição da `Confirmação da rodada` foram substituídos por novos SVGs customizados mais legíveis e mais próximos do estilo flat desejado, melhorando a presença visual nos botões, anéis e cards de status.
- Os ícones de bola e refeição da `Confirmação da rodada` deixaram de usar SVG manual e passaram a usar `react-icons`, com `MdSportsSoccer` e `MdOutlineRestaurantMenu`, alinhando a interface à biblioteca visual pedida e melhorando a consistência dos pictogramas.
- A modal operacional do jogo foi separada em duas etapas para coordenação: primeiro `Confirmação da rodada`; depois `Jogo e escalação`. O avanço agora acontece automaticamente após `Salvar minha confirmação`, mantendo a leitura mais limpa e sem empilhar confirmação e operação na mesma tela de uma vez.
- Na etapa inicial `Confirmação da rodada` da coordenação, o bloco `Sua Resposta Recente` foi ocultado; a área inferior de leitura extra não aparece mais antes do salvamento, deixando a primeira tela focada apenas na confirmação. A parte de placar/tempo/escalação continua aparecendo somente após salvar.
- A etapa `Jogo e escalação` da modal operacional foi redesenhada para um board visual mais próximo da referência: card de tempo/placar no topo, laterais com titulares e reservas de cada time, gramado central com posicionamento dos atletas e rodapé separado entre log de eventos e ações de fechamento da súmula.
- A solicitação seguinte removeu toda a parte inferior da modal operacional novamente: ao abrir a súmula, a interface agora exibe somente a `Confirmação da rodada`, sem renderizar mais o bloco abaixo dela. A reconstrução da área de jogo/escalação ficará para uma próxima iteração partindo do zero.
- Depois disso foi identificado um segundo caminho legado no dashboard (`DashboardMatchesPanel`) que ainda mostrava a área operacional antiga da súmula. A home da temporada passou a usar o `MatchesPanel` já limpo, apagando essa parte antiga do site na origem sem depender de novos escondes locais.
- Em seguida foi necessário voltar o topo da temporada para o `DashboardMatchesPanel`, porque a troca para `MatchesPanel` removeu a paleta visual anterior e o gramado ao fundo do hero. O `DashboardMatchesPanel` foi mantido, mas sua modal foi limpa para exibir apenas a `Confirmação da rodada`, sem reativar a parte operacional antiga.
- Depois disso, a modal `Abrir súmula` do coordenador foi reativada como board operacional dentro do `DashboardMatchesPanel`, combinando novamente placar/tempo, checklist, trocas, editor de escalação já expandido, campo central e fechamento da súmula. O hero da temporada com gramado e paleta anterior foi preservado.
- A decisão seguinte separou novamente os conceitos: a modal ativa do `DashboardMatchesPanel` voltou a mostrar apenas `Confirmação da rodada`, sem board operacional, sem `Abrir súmula` embutida e sem misturar confirmação com jogo/escalação. O hero com gramado e paleta anterior foi preservado.
- Na sequência, o card do próximo jogo passou a separar os gatilhos: `Confirmações` continua abrindo a modal de `Confirmação da rodada`, enquanto `Abrir súmula` abre uma segunda modal independente para coordenação, deixada vazia por enquanto para reconstrução posterior sem contaminar o fluxo de presença.
- Depois disso, a nova modal de `Abrir súmula` ganhou um board próprio no `DashboardMatchesPanel`, separado das confirmações: topo claro com cronômetro/placar central, colunas de titulares e banco, campo tático verde ao centro, área de troca à esquerda e bloco de fechamento/log/ações à direita. Os botões `Salvar Súmula`, `Iniciar Relatório`, `FINALIZAR JOGO` e `GERAR SÚMULA` foram ligados ao fluxo real de rascunho, início, submissão e confirmação.
- A modal `Abrir súmula` foi ampliada e compactada para aproveitar mais a viewport e reduzir barras de rolagem. Quando a escalação ainda não existe no banco, o board agora semeia automaticamente os atletas a partir das confirmações salvas, distribui os confirmados entre `TIME A` e `TIME B` com o balanceador existente e já mostra esses nomes nas listas laterais correspondentes.
- O botão de `Confirmações`/`Confirmar presença` no card principal do próximo jogo foi restaurado como ação sempre visível acima de `Abrir súmula`, sem depender de a janela estar aberta naquele instante. Assim a modal de confirmação continua acessível o tempo todo.
- A aba `Mensalidades` deixou o grid de cards individuais e passou a exibir uma tabela densa ordenada por nome/mês, reunindo nome, valor, pago, pendente, status, ponto, observação e ação operacional em menos espaço vertical.
- O bloco `Caixa do grupo` dentro de `Mensalidades` também deixou os cards e passou a usar tabela compacta com data, tipo, descrição, origem, responsável e valor.
- As tabelas de `Mensalidades` e `Caixa do grupo` receberam uma segunda linha no cabeçalho com filtros por coluna, permitindo pesquisa direcionada por nome, mês, vencimento, status, observação, descrição, origem, responsável e demais campos visíveis.
- O filtro dessas tabelas foi refinado para o padrão solicitado: cada coluna agora mostra um ícone de filtro no cabeçalho; ao clicar, abre um painel lateral compacto com campo de busca e lista rolável de valores daquele campo para seleção direta.
- Os indicadores financeiros do módulo `Mensalidades` foram reunidos em uma única faixa horizontal, mantendo recebido, aberto, pendente, atraso, pontos antecipados, receitas, despesas e saldo de caixa na mesma linha com rolagem lateral quando necessário.
- O fluxo de mensalistas foi blindado contra duplicidade por e-mail: cadastro/edição agora recusam e-mails já existentes, a geração/listagem de mensalidades desconsidera atletas repetidos por e-mail e foi preparada uma migration SQL manual para consolidar pagamentos duplicados e inativar cadastros repetidos legados.

## Regras importantes consolidadas

- Não usar ORM.
- Não criar `.env`.
- Backend deve subir somente com `NODE_ENV=production` e `PORT=8080`.
- Não criar dados mockados.
- Toda alteração de banco deve ser SQL manual em `/migrations`.
- Para bancos já existentes com usuários cadastrados em `LINHA`/`GOLEIRO`, executar `migrations/04_posicoes_oficiais_atletas.sql`; a migração converte `GOLEIRO` para `GO`, `LINHA` para `MC` e preserva posições oficiais já válidas.
- Para continuidade da temporada 2026, executar também `migrations/03_saldo_inicial_temporada_excel.sql` antes de importar a tabela do Excel.
- `Vera Verão` foi removido.
- Cartões oficiais: amarelo, vermelho e azul.
- Controle de cartões usa pontos ponderados no ranking: amarelo = 1, azul = 2, vermelho = 3.
- Vermelho suspende 1 jogo.
- 3 amarelos acumulados em jogos confirmados da temporada suspendem 1 jogo, inclusive 1 amarelo em um jogo + 2 amarelos no jogo seguinte.
- O histórico oficial é acumulado por várias temporadas e forma a carreira do atleta: estatísticas, títulos, prêmios e badges permanecem vinculados à temporada de origem.
- Mensalidade pontua apenas quando paga antes do vencimento (`paid_at::date < due_date`) e vinculada a uma temporada.
- Awards/badges definidos: rankings automáticos de temporada, votação sigilosa e badges históricos por atleta.
- `SELECAO_ANO`: consolidação gera placement 1 para goleiro e placements 2 a 7 para os seis jogadores de linha mais votados.
- A tabela da temporada é calculada por: saldo inicial importado do Excel + súmulas confirmadas/corrigidas + mensalidades pagas antes do vencimento.
- Pontuação da presença: quem joga soma o ponto de participação via `games_played`; quem comparece e não joga soma o mesmo ponto através de `presences`.

## Validações executadas

- `backend`: `npm install`, `npm run typecheck` e `npm run build` concluídos com sucesso após ajustar a validação do `start` para considerar apenas titulares em quadra e normalizar o papel operacional salvo.
- `backend`: `npm run typecheck`, `npm run build` e `npm audit --audit-level=moderate` concluídos com sucesso após hardening P1.
- `backend`: `npm run typecheck` e `npm run build` concluídos com sucesso após criação da migration `10` e blindagem do `GET /matches/:id` contra ausência temporária de `match_corrections`.
- `backend`: `npm run typecheck` e `npm run build` concluídos com sucesso após corrigir a sintaxe PostgreSQL do cálculo de `availableMinutes` em `/matches/:id`.
- `backend`: `npm run typecheck` e `npm run build` concluídos com sucesso após adicionar `/auth/change-password`.
- `frontend`: `npm run typecheck`, `npm run build` e `npm audit --audit-level=moderate` concluídos com sucesso após ajustes de árbitro, responsividade e runtime config Railway.
- `frontend`: `npm run typecheck` e `npm run build` concluídos com sucesso após adicionar `autocomplete` nos inputs de senha.
- `frontend`: `npm run typecheck` e `npm run build` concluídos com sucesso após remover `Primeiro acesso` do login, centralizar o card e adicionar a modal `Trocar senha`.
- `frontend`: `npm run typecheck` e `npm run build` concluídos com sucesso após remover a aba `perfis`, criar o modal global de perfil e tornar nomes de atletas da classificação/rankings clicáveis.
- `frontend`: `npm run typecheck` e `npm run build` concluídos com sucesso após integrar jogos na tela da temporada, mover o detalhe da súmula para modal fumê operacional, renderizar lançamento rápido por atleta, ajustar microcopy de criação de jogo e ajustar o logo `POKA PRÁTIKA`.
- `frontend`: `npm run typecheck` e `npm run build` concluídos com sucesso após migrar mensalidades para dashboard com modais de geração/registro e mover configurações de prêmios para modal fumê.
- `frontend`: `npm run typecheck` e `npm run build` concluídos com sucesso após reorganizar `config.` em home administrativa com modais para temporada, pontuação, usuário, importação Excel e edição individual de usuários.
- `frontend`: `npm run typecheck` e `npm run build` concluídos com sucesso após remover a bolinha amarela do logo e baixar `POKA`/`PRÁTIKA` em 2px no SVG.
- `backend`: `npm run typecheck` e `npm run build` concluídos com sucesso após a reforma final de UI para validar o conjunto full-stack antes da homologação.
- `backend`: `npm audit --audit-level=moderate` sem vulnerabilidades.
- `frontend`: `npm audit --audit-level=moderate` sem vulnerabilidades.
- Checagem do workspace sem erros ativos, sem `.env`, sem ORM operacional e sem `console.log`/`window.confirm`/`alert` operacional.
- `frontend`: remoção do pop-up global automático de confirmação de presença pendente; o acesso à confirmação ficou restrito ao card/modal do jogo e exige nova validação com `npm run typecheck`.
- `frontend`: refatoração visual do dashboard validada sem erros no editor em `frontend/src/App.tsx` e `frontend/src/styles.css`; `npm run typecheck` continua indisponível no ambiente atual porque `tsc` não está acessível via terminal do frontend.
- `frontend`: `npm install react-soccer-lineup` executado com sucesso para sustentar o fundo do campo no dashboard; diagnósticos do editor seguem sem erros em `frontend/src/App.tsx` e `frontend/src/styles.css`.
- `frontend`: `npm run typecheck` e `npm run build` concluídos com sucesso após o refinamento fino do dashboard principal alinhado à imagem de referência; o build ainda reporta um warning pré-existente de `@import` fora do topo em `frontend/src/styles.css`.
- `frontend`: `npm run build` concluído com sucesso após ampliar a ocupação horizontal do dashboard e substituir o fundo do hero por um único campo grande em verde escuro; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após remover o `pattern="lines"` do campo da biblioteca, centralizar um único gramado maior no hero e ampliar as áreas laterais do topo para eliminar cortes de status e botões; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após padronizar os botões de fechamento de modal como `X`; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após redesenhar a modal `Súmula Inteligente` para o novo layout visual em wizard; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após adicionar ícones e comprimir a `Súmula Inteligente` para eliminar a rolagem do modal principal; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após converter o stepper da `Súmula Inteligente` para o visual em pílulas conectadas; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após fazer a pílula do stepper da `Súmula Inteligente` envolver número + rótulo completo; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após compactar a lista de atletas selecionados da `Súmula Inteligente` para linhas únicas; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após dividir a lista de atletas selecionados da `Súmula Inteligente` em duas colunas quando houver mais de 6 nomes; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após deixar a lista de atletas selecionados da `Súmula Inteligente` sempre em duas colunas no desktop; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após simplificar o resultado do sorteio da `Súmula Inteligente` para linhas compactas clicáveis que alternam banco; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após mover o resultado do sorteio da `Súmula Inteligente` para um modal rápido de confirmação dos times; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após redesenhar a `Confirmação da rodada` para o layout da referência e reparar a estrutura interna do `App.tsx`; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após substituir os ícones de bola e refeição da `Confirmação da rodada`; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm install react-icons` executado e `npm run build` concluído com sucesso após migrar os ícones de bola e refeição da `Confirmação da rodada` para `react-icons`; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após separar a modal do jogo em duas etapas para coordenação (`Confirmação da rodada` → `Jogo e escalação`) com avanço automático ao salvar a confirmação; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após ocultar `Sua Resposta Recente` na etapa inicial de confirmação da coordenação, mantendo o avanço para `Jogo e escalação` somente depois do salvamento; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após redesenhar a etapa `Jogo e escalação` em formato de board operacional com gramado central e laterais de elenco; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após remover novamente toda a parte inferior da modal operacional e deixar apenas a `Confirmação da rodada`; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após reorganizar a aba `Mensalidades` em tabela ordenada por nome; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `backend`: `npm run build` e `frontend`: `npm run build` devem ser executados após a blindagem de mensalistas duplicados e a criação da migration manual de deduplicação por e-mail.
- `frontend`: `npm run build` concluído com sucesso após trocar a home da temporada de `DashboardMatchesPanel` para `MatchesPanel`, removendo do site o caminho legado que ainda renderizava a antiga área operacional da súmula; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após restaurar o hero visual de `DashboardMatchesPanel` (cores e gramado ao fundo) e manter sua modal limitada à `Confirmação da rodada`; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após restaurar a modal `Abrir súmula` do coordenador como board operacional e manter o hero visual de `DashboardMatchesPanel`; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após remover novamente o board de `Abrir súmula` da modal ativa e voltar o fluxo para `Confirmação da rodada` apenas; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após separar os cliques de `Confirmações` e `Abrir súmula` em modais diferentes no `DashboardMatchesPanel`; a modal nova da súmula ficou vazia por enquanto e o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após implementar o board visual dedicado da nova modal `Abrir súmula`, com cronômetro/placar, elencos laterais, campo tático, trocas e fechamento funcional; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após ampliar a modal `Abrir súmula`, compactar o board e semear automaticamente os times a partir das confirmações quando não houver escalação salva; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.
- `frontend`: `npm run build` concluído com sucesso após restaurar no card principal o botão sempre visível de `Confirmações`/`Confirmar presença` acima de `Abrir súmula`; o warning pré-existente de `@import` fora do topo em `frontend/src/styles.css` permanece sem bloquear o bundle.

## Próximo passo técnico recomendado

1. Em banco novo, executar `migrations/01_core_schema.sql` e depois as migrações incrementais aplicáveis em ordem crescente.
2. Em banco já existente, executar até `migrations/10_correcoes_auditadas_sumula.sql`, garantindo também `04`, `05`, `06`, `07`, `08` e `09` se ainda não tiverem sido aplicadas.
3. Subir backend e frontend na Railway com root directories corretos; para o erro atual de abertura de súmula, redeploy do backend é obrigatório porque a correção está no SQL da rota `/matches/:id`.
4. Conferir variáveis Railway: backend com `DATABASE_URL`, `NODE_ENV=production`, `PORT=8080`, `JWT_SECRET`, `ALLOWED_ORIGINS`, `FRONTEND_URL` e credenciais Microsoft Graph; frontend com `VITE_API_URL`.
5. Garantir que já exista pelo menos um ADMIN ativo; em banco totalmente novo, usar o bootstrap seguro do backend de forma controlada antes de liberar o acesso público.
6. Cadastrar atletas reais por e-mail e deixar o sistema enviar o convite de ativação via Microsoft Graph.
7. Criar/abrir a temporada 2026 pelo painel `config.`.
8. Importar a tabela atual do Excel no painel administrativo usando preferencialmente a coluna `email` para casar cada linha com o usuário certo.
9. Conferir classificação, rankings, suspensões e cartões; depois usar apenas as novas súmulas confirmadas/corrigidas para continuidade da temporada.
10. Próxima etapa técnica: redeploy na Railway, executar as migrations pendentes no PostgreSQL em ordem e homologar os fluxos reais de aceite: login, trocar senha, criar jogo, iniciar súmula, lançar eventos rápidos, confirmar pontuação, registrar mensalidade, votar prêmios e operar `config.`.
